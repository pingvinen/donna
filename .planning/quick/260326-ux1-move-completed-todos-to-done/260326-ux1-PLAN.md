---
phase: quick
plan: 260326-ux1
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/todos/pending/2026-03-14-comment-on-prs-after-release-with-version-number.md
  - .planning/todos/done/2026-03-14-comment-on-prs-after-release-with-version-number.md
  - .planning/STATE.md
autonomous: true
must_haves:
  truths:
    - "No completed TODO files remain in pending/"
    - "STATE.md Pending Todos list matches actual pending/ folder contents"
  artifacts:
    - path: ".planning/todos/done/2026-03-14-comment-on-prs-after-release-with-version-number.md"
      provides: "Completed TODO moved to done folder"
    - path: ".planning/STATE.md"
      provides: "Updated pending todos list"
  key_links: []
---

<objective>
Move completed TODO files from pending/ to done/ and synchronize STATE.md's Pending Todos section with actual pending/ folder contents.

Purpose: Housekeeping per CLAUDE.md step 9 — keep planning state accurate after Phase 4 completion.
Output: Clean pending/ folder (only genuinely pending items), accurate STATE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-ingest-github-issues-into-gsd/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move completed TODO to done/ and update STATE.md</name>
  <files>.planning/todos/pending/2026-03-14-comment-on-prs-after-release-with-version-number.md, .planning/todos/done/2026-03-14-comment-on-prs-after-release-with-version-number.md, .planning/STATE.md</files>
  <action>
1. Move the completed TODO file:
   - `git mv .planning/todos/pending/2026-03-14-comment-on-prs-after-release-with-version-number.md .planning/todos/done/`
   - This TODO (ref: #21, "Comment on PRs after release with version number") was completed by Phase 4 Plan 02 which implemented `scripts/post-release-comments.cjs` and integrated it into `release.yml`.

2. Update STATE.md `### Pending Todos` section to match the actual pending/ folder contents. The current list is stale — it includes two items that no longer exist as files ("Store GraphQL schemas for real diff-based relearn" and "Restructure tools data format") and is missing 9 new TODOs added on 2026-03-26.

   Replace the `### Pending Todos` block with this accurate list derived from the actual pending/ folder:

   ```
   ### Pending Todos

   - Clean up removed files on upgrade (tooling)
   - Evaluate natural language input as alternative to slash commands (general)
   - Handle timezone changes in daily files (general)
   - Add task priority and sorting configuration for daily briefs (general)
   - Check for new Donna version once per day (tooling)
   - Skip setup prompt when Donna is already configured (tooling)
   - Simplify adjust-tool — remove type change support (tooling)
   - Add ASCII art branding to Donna's output banners (general, ref: #13)
   - Add GitHub workflow that blocks merging if UAT not finalized (ci, ref: #27)
   - Document why automated periodic run-tools invocations are not supported (docs, ref: #23)
   - Extract possible tasks from meeting notes and transcripts (tooling, ref: #25)
   - Make the skills list in README easier to comprehend with grouping (docs, ref: #22)
   - Enhance tool learning to read source code, docs, and API schemas (tooling, ref: #20)
   - Make UAT easier with sandbox environment and test tools (testing, ref: #19)
   - Refactor skill bootstrap (config, migrations) into reusable module (tooling, ref: #30)
   - Stop wrapping commands in "timeout" if timeout is not installed (general, ref: #18)
   ```

3. Also update `stopped_at` in STATE.md frontmatter to reflect this cleanup:
   - Change `stopped_at` to: "Completed 04-ingest-github-issues-into-gsd. State cleanup done."

4. Stage all changes with `git add`.
  </action>
  <verify>
    <automated>test ! -f .planning/todos/pending/2026-03-14-comment-on-prs-after-release-with-version-number.md && test -f .planning/todos/done/2026-03-14-comment-on-prs-after-release-with-version-number.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Completed TODO moved to done/, STATE.md Pending Todos section lists exactly the 16 items currently in pending/ (no phantom entries, no missing entries), changes staged.</done>
</task>

</tasks>

<verification>
- `ls .planning/todos/pending/ | wc -l` returns 16 (was 17)
- `ls .planning/todos/done/ | wc -l` returns 12 (was 11)
- `grep -c "^-" .planning/STATE.md` in the Pending Todos section matches 16
- No "Store GraphQL schemas" or "Restructure tools data format" lines in STATE.md
- No "Comment on PRs after release" in pending/
</verification>

<success_criteria>
- Zero completed TODOs remain in pending/
- STATE.md Pending Todos section is an accurate reflection of pending/ folder
- All changes staged for commit
</success_criteria>

<output>
After completion, create `.planning/quick/260326-ux1-move-completed-todos-to-done/260326-ux1-SUMMARY.md`
</output>
