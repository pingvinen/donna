---
phase: 01-low-hanging-documentation-stuff-for-users-and-alpha-testers
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - CONTRIBUTING.md
  - src/changelog.cjs
  - test/installer.test.cjs
autonomous: true
requirements: [DOC-01, DOC-02]
gap_closure: true

must_haves:
  truths:
    - "CONTRIBUTING.md explains GSD workflow and backlog-driven development approach"
    - "CONTRIBUTING.md surfaces all Development Approach conventions from CLAUDE.md"
    - "Installer upgrade from 0.4.0 to 0.5.0 shows 'What's new:' changelog section"
  artifacts:
    - path: "CONTRIBUTING.md"
      provides: "Development Workflow section covering GSD and no-milestones approach"
      contains: "GSD"
    - path: "src/changelog.cjs"
      provides: "0.5.0 changelog entry with new skills and improvements"
      contains: "0.5.0"
  key_links:
    - from: "src/changelog.cjs"
      to: "src/installer.cjs"
      via: "displayChangelog called during upgrade"
      pattern: "displayChangelog"
---

<objective>
Close two UAT gaps from phase 01: (1) add a Development Workflow section to CONTRIBUTING.md explaining GSD and backlog-driven development, and (2) populate the 0.5.0 CHANGELOG entry so installer upgrades show "What's new:".

Purpose: UAT revealed CONTRIBUTING.md omits the project's core development workflow, and the changelog system works but has no data.
Output: Updated CONTRIBUTING.md, populated changelog, updated test expectations.
</objective>

<execution_context>
@/Users/pingvinen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pingvinen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Development Workflow section to CONTRIBUTING.md</name>
  <files>CONTRIBUTING.md</files>
  <action>
Read CONTRIBUTING.md. Insert a new "## Development Workflow" section between "## Running Tests" and "## Adding a New Skill". The section must cover:

1. **GSD (Get Shit Done) workflow** -- Donna uses the GSD planning framework (`.planning/` directory) for all development. Phases are planned, researched, executed, and verified using GSD orchestrator commands. New contributors should understand that work is organized into phases with plans, not ad-hoc.

2. **Backlog-driven, no formal milestones** -- Surface the Development Approach from CLAUDE.md:
   - Deployment first: build packaging/distribution/CI before features. Every phase produces distributable artifacts.
   - Real skills, not throwaway dummies: when a placeholder is needed, stub a real skill.
   - No formal milestones: work from a backlog, ship when value accumulates. Releases happen organically.

3. **Phase artifacts** -- Briefly note that `.planning/phases/` contains phase plans, summaries, and verification artifacts. Contributors should read existing phase summaries to understand prior decisions.

Keep the section concise (roughly 20-30 lines of markdown). Match the existing document's tone and formatting style. Do NOT rewrite other sections.
  </action>
  <verify>
    <automated>grep -c "GSD" CONTRIBUTING.md | xargs test 0 -lt && grep -c "backlog" CONTRIBUTING.md | xargs test 0 -lt && grep -c "Development Workflow" CONTRIBUTING.md | xargs test 0 -lt && echo "PASS"</automated>
  </verify>
  <done>CONTRIBUTING.md contains a Development Workflow section that explains GSD phases and backlog-driven development with no formal milestones</done>
</task>

<task type="auto">
  <name>Task 2: Populate 0.5.0 CHANGELOG entry and update test expectations</name>
  <files>src/changelog.cjs, test/installer.test.cjs</files>
  <action>
**src/changelog.cjs:** Replace the empty CHANGELOG object (and the commented-out example) with a real entry:

```js
const CHANGELOG = {
    "0.5.0": {
        "New skills": [
            "donna:help — conversational troubleshooting and diagnostics",
            "donna:contribute-idea — submit feature ideas via GitHub Issues",
        ],
        "Improvements": [
            "CONTRIBUTING.md developer guide added",
            "Upgrade changelog shown during version bumps",
        ],
    },
};
```

**test/installer.test.cjs:** Find the test "calls changelog display during upgrade without throwing" (around line 453). It currently asserts that "What's new:" does NOT appear because CHANGELOG was empty. Update it to assert that "What's new:" DOES appear during upgrade. The test comment also needs updating to reflect that CHANGELOG now has entries.

Do NOT modify the "does not show What's new on fresh install" test -- that should still pass as-is since fresh installs don't call displayChangelog.

Do NOT modify the "does not throw with empty CHANGELOG" unit test in the "changelog - displayChangelog" describe block -- that tests displayChangelog directly with the empty state and should still work because it calls displayChangelog directly (not through the CHANGELOG constant).

Run `node --test 'test/*.test.cjs'` to confirm all tests pass after changes.
  </action>
  <verify>
    <automated>cd /Users/pingvinen/workspace/github/donna && node --test 'test/*.test.cjs' 2>&1 | tail -5</automated>
  </verify>
  <done>src/changelog.cjs has a 0.5.0 entry with new skills and improvements. Installer upgrade test confirms "What's new:" appears. All tests pass.</done>
</task>

</tasks>

<verification>
1. `grep "GSD" CONTRIBUTING.md` returns matches
2. `grep "backlog" CONTRIBUTING.md` returns matches
3. `grep "0.5.0" src/changelog.cjs` returns match in CHANGELOG object
4. `node --test 'test/*.test.cjs'` -- all tests pass with 0 failures
</verification>

<success_criteria>
- CONTRIBUTING.md has a Development Workflow section covering GSD and no-milestones approach
- src/changelog.cjs CHANGELOG object has a "0.5.0" entry with new skills and improvements
- Installer upgrade path shows "What's new:" section when upgrading to 0.5.0
- All existing tests pass (245+ tests, 0 failures)
</success_criteria>

<output>
After completion, create `.planning/phases/01-low-hanging-documentation-stuff-for-users-and-alpha-testers/01-04-SUMMARY.md`
</output>
