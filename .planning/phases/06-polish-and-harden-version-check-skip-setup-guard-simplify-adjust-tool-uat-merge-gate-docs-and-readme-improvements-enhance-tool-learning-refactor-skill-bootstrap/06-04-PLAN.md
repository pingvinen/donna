---
phase: 06-polish-and-harden
plan: 04
type: execute
wave: 2
depends_on: [06-02]
files_modified:
  - workflows/add-tool.md
  - workflows/relearn-tools.md
autonomous: true
requirements: [D-09]

must_haves:
  truths:
    - "add-tool learn-capabilities step tries local docs before --help for unknown CLI tools"
    - "add-tool learn-capabilities step offers web docs fetch if local docs insufficient"
    - "add-tool learn-capabilities step asks user before analyzing source code"
    - "relearn-tools relearn-changed step uses the same cascading approach as add-tool"
    - "Existing well-known tool baselines (gh, jira, kubectl) are unchanged"
    - "Existing GraphQL introspection flow is unchanged"
  artifacts:
    - path: "workflows/add-tool.md"
      provides: "Enhanced learn-capabilities with cascading sources"
      contains: "local docs"
    - path: "workflows/relearn-tools.md"
      provides: "Enhanced relearn-changed with cascading sources"
      contains: "local docs"
  key_links:
    - from: "workflows/add-tool.md learn-capabilities"
      to: "local README/docs, web docs, source code"
      via: "cascading fallback chain"
      pattern: "README\\|docs\\|source code"
---

<objective>
Enhance the tool learning cascade in both add-tool and relearn-tools workflows per D-09: add local docs, web docs, and user-opt-in source code analysis as richer capability discovery sources on top of the existing --help baseline.

Purpose: Unknown CLI tools currently only get `--help` parsing. The cascade adds three richer sources (local docs -> web docs -> source code with user opt-in) to discover more capabilities.

Output: Updated `add-tool.md` and `relearn-tools.md` with cascading learning logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-and-harden-version-check-skip-setup-guard-simplify-adjust-tool-uat-merge-gate-docs-and-readme-improvements-enhance-tool-learning-refactor-skill-bootstrap/06-CONTEXT.md
@.planning/phases/06-polish-and-harden-version-check-skip-setup-guard-simplify-adjust-tool-uat-merge-gate-docs-and-readme-improvements-enhance-tool-learning-refactor-skill-bootstrap/06-RESEARCH.md

<interfaces>
From workflows/add-tool.md learn-capabilities step (current unknown tool logic):
```markdown
For **unknown tools**, run `<command> --help 2>&1 | head -80` via Bash and use Claude's
understanding to identify 3-5 capabilities relevant to daily task management.
```

From workflows/relearn-tools.md relearn-changed step (current unknown tool logic):
```markdown
For **unknown tools**, run `<command> --help 2>&1 | head -80` via Bash and use Claude's
understanding to identify 3-5 capabilities relevant to daily task management.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cascading learning to add-tool learn-capabilities step</name>
  <files>workflows/add-tool.md</files>
  <read_first>
    - workflows/add-tool.md (full file — especially the learn-capabilities step, well-known tool baselines for gh/jira/kubectl, unknown tool --help logic, REST/GraphQL/MCP sections)
  </read_first>
  <action>
    In `workflows/add-tool.md`, modify the `<step name="learn-capabilities">` section. The well-known tool baselines (gh, jira, kubectl) and the REST/GraphQL/MCP sections remain unchanged. Only the **unknown CLI tool** learning logic changes.

    Replace the current unknown tool block:
    ```
    For **unknown tools**, run `<command> --help 2>&1 | head -80` via Bash and use Claude's
    understanding to identify 3-5 capabilities relevant to daily task management.
    ```

    With the following cascading approach per D-09:

    ```markdown
    For **unknown tools**, use a cascading approach to learn capabilities. Each stage builds on the previous. Stop when 3-5 relevant capabilities have been identified.

    **Stage 1 — Local docs:**
    Attempt to find local documentation for the tool:
    ```bash
    TOOL_PATH=$(which <command> 2>/dev/null)
    ```
    If found, check for README or docs in the tool's package directory:
    ```bash
    TOOL_DIR=$(dirname "$(dirname "$TOOL_PATH")")
    ls "$TOOL_DIR"/README* "$TOOL_DIR"/doc* "$TOOL_DIR"/docs* 2>/dev/null | head -5
    ```
    If doc files exist, read up to 200 lines from the most relevant one (prefer README, then docs/). Use Claude's understanding to identify capabilities relevant to daily task management.

    **Stage 2 — CLI help (baseline):**
    Run `<command> --help 2>&1 | head -80` via Bash. Combine with any Stage 1 findings. Use Claude's understanding to identify 3-5 capabilities relevant to daily task management. If `<scope>` is set, incorporate the scope into CLI invocations.

    **Stage 3 — Web docs (if stages 1-2 found fewer than 3 capabilities):**
    If fewer than 3 capabilities identified so far, attempt to fetch the tool's documentation from the web. Use WebFetch on common doc URLs:
    - `https://<command>.dev` or `https://<command>.io`
    - The homepage URL from `<command> --help` output if one was printed

    If a docs page is found, extract additional capabilities.

    **Stage 4 — Source code analysis (user opt-in per D-09):**
    After stages 1-3, if the tool path was found in Stage 1, print the number of capabilities discovered and ask the user:

    Use AskUserQuestion:
    ```
    Found <N> capabilities from docs and help output. Want me to analyze <command>'s source code for more?
    ```

    If the user says yes:
    - Read up to 500 lines from the main entry point or lib/ directory of the tool
    - Identify additional capabilities from function names, subcommands, or API surface
    - Add any new relevant capabilities to the list

    If the user says no, continue with what was found.

    Format each capability as `name: <cli invocation>`.
    ```

    Important: Keep the existing note about incorporating `<scope>` into CLI invocations. The well-known tools section, REST section, GraphQL section, and MCP section must remain completely unchanged.
  </action>
  <verify>
    <automated>grep -c "Stage 1\|Stage 2\|Stage 3\|Stage 4\|local docs\|source code" workflows/add-tool.md</automated>
  </verify>
  <acceptance_criteria>
    - workflows/add-tool.md contains "Stage 1" and "Local docs"
    - workflows/add-tool.md contains "Stage 2" and "CLI help"
    - workflows/add-tool.md contains "Stage 3" and "Web docs"
    - workflows/add-tool.md contains "Stage 4" and "source code analysis"
    - workflows/add-tool.md contains "Want me to analyze" (user opt-in prompt per D-09)
    - workflows/add-tool.md still contains "gh (GitHub CLI)" (well-known baselines preserved)
    - workflows/add-tool.md still contains "jira" and "kubectl" (well-known baselines preserved)
    - workflows/add-tool.md still contains "If `<tool_type>` is `rest`" (REST section preserved)
    - workflows/add-tool.md still contains "If `<tool_type>` is `graphql`" (GraphQL section preserved)
    - workflows/add-tool.md still contains "If `<tool_type>` is `mcp`" (MCP section preserved)
  </acceptance_criteria>
  <done>add-tool learn-capabilities uses cascading approach for unknown tools: local docs, --help, web docs, and user-opt-in source code analysis. Well-known baselines and non-CLI sections unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add cascading learning to relearn-tools relearn-changed step</name>
  <files>workflows/relearn-tools.md</files>
  <read_first>
    - workflows/relearn-tools.md (full file — especially the relearn-changed step, well-known tool baselines, and the note about not asking user to re-select capabilities)
    - workflows/add-tool.md (the updated learn-capabilities step from Task 1 — to mirror the cascade)
  </read_first>
  <action>
    In `workflows/relearn-tools.md`, modify the `<step name="relearn-changed">` section. The well-known tool baselines (gh, jira, kubectl) remain unchanged. Only the unknown tool learning logic changes.

    Replace the current unknown tool block in relearn-changed:
    ```
    For **unknown tools**, run `<command> --help 2>&1 | head -80` via Bash and use Claude's
    understanding to identify 3-5 capabilities relevant to daily task management.
    ```

    With the same cascading approach added to add-tool in Task 1, but adapted for relearn context:

    ```markdown
    For **unknown tools**, use a cascading approach to re-learn capabilities. Each stage builds on the previous. The goal is to update CLI invocations for existing capability names — do NOT ask the user to re-select capabilities.

    **Stage 1 — Local docs:**
    Attempt to find local documentation for the tool:
    ```bash
    TOOL_PATH=$(which <command> 2>/dev/null)
    ```
    If found, check for README or docs in the tool's package directory:
    ```bash
    TOOL_DIR=$(dirname "$(dirname "$TOOL_PATH")")
    ls "$TOOL_DIR"/README* "$TOOL_DIR"/doc* "$TOOL_DIR"/docs* 2>/dev/null | head -5
    ```
    If doc files exist, read up to 200 lines from the most relevant one. Use to update invocations for existing capability names.

    **Stage 2 — CLI help (baseline):**
    Run `<command> --help 2>&1 | head -80` via Bash. Combine with Stage 1 findings. Update invocations for existing capability names based on any changes to flags, subcommands, or output formats.

    **Stage 3 — Web docs (if invocations could not be updated from stages 1-2):**
    If any existing capability's invocation could not be validated from local docs or help output, attempt to fetch the tool's web documentation. Use WebFetch on common doc URLs:
    - `https://<command>.dev` or `https://<command>.io`
    - The homepage URL from `<command> --help` output if one was printed

    **Stage 4 — Source code analysis (user opt-in per D-09):**
    After stages 1-3, if the tool path was found, ask the user:

    Use AskUserQuestion:
    ```
    Updated <N> capability invocations from docs and help output. Want me to analyze <command>'s source code for additional capabilities?
    ```

    If yes: read source, identify new capabilities, add to the list.
    If no: continue with updated invocations.
    ```

    Important: Preserve the existing note "Do NOT ask the user to re-select capabilities -- keep the same capability names, update only the CLI invocations". The cascade's Stage 4 is the only place where NEW capabilities might be added (with user opt-in).

    Also preserve the note about REST/MCP tools: "re-learning not applicable (capabilities are user-defined)". The cascade applies to CLI unknown tools only.
  </action>
  <verify>
    <automated>grep -c "Stage 1\|Stage 2\|Stage 3\|Stage 4\|local docs\|source code" workflows/relearn-tools.md</automated>
  </verify>
  <acceptance_criteria>
    - workflows/relearn-tools.md contains "Stage 1" and "Local docs"
    - workflows/relearn-tools.md contains "Stage 2" and "CLI help"
    - workflows/relearn-tools.md contains "Stage 3" and "Web docs"
    - workflows/relearn-tools.md contains "Stage 4" and "source code analysis"
    - workflows/relearn-tools.md contains "Want me to analyze" (user opt-in per D-09)
    - workflows/relearn-tools.md still contains "Do NOT ask the user to re-select capabilities"
    - workflows/relearn-tools.md still contains "gh (GitHub CLI)" (well-known baselines preserved)
    - workflows/relearn-tools.md still contains "re-learning not applicable" (REST/MCP note preserved)
  </acceptance_criteria>
  <done>relearn-tools uses cascading approach for unknown CLI tools, matching the add-tool pattern. User opt-in required for source code analysis. Existing baselines and constraints preserved.</done>
</task>

</tasks>

<verification>
- Both workflows contain the 4-stage cascade for unknown tools
- Well-known tool baselines unchanged in both files
- Source code analysis requires user opt-in (AskUserQuestion) in both files
- REST/GraphQL/MCP learning paths unchanged
</verification>

<success_criteria>
- add-tool and relearn-tools both use cascading learning: local docs -> --help -> web docs -> source code (opt-in)
- Well-known tools still use training data baselines (no regression)
- Source code analysis always asks user first per D-09
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-and-harden-version-check-skip-setup-guard-simplify-adjust-tool-uat-merge-gate-docs-and-readme-improvements-enhance-tool-learning-refactor-skill-bootstrap/06-04-SUMMARY.md`
</output>
