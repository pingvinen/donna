---
phase: 06-polish-and-harden
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [D-07, D-08]

must_haves:
  truths:
    - "README skills are grouped into logical categories with clear headings"
    - "README explains why automated periodic run-tools invocations are not supported"
    - "All existing command descriptions are preserved (no information lost)"
  artifacts:
    - path: "README.md"
      provides: "Grouped skills list and automation docs"
      contains: "## All commands"
  key_links: []
---

<objective>
Improve README.md documentation: (1) group the skills list into logical categories per D-07, (2) add documentation explaining why automated periodic run-tools is not supported per D-08.

Purpose: Makes the skills list easier to comprehend for new users and addresses a recurring question about automation.

Output: Updated README.md with grouped skills table and new documentation section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-and-harden-version-check-skip-setup-guard-simplify-adjust-tool-uat-merge-gate-docs-and-readme-improvements-enhance-tool-learning-refactor-skill-bootstrap/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Group skills list and add automation documentation in README</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — current skills table at lines 129-144, section structure)
  </read_first>
  <action>
    **D-07: Group skills list into categories.**

    Replace the flat "All commands" table (lines 129-144) with a grouped structure. Use sub-tables with category headings. Preserve every existing command and description — this is a layout change, not a content change.

    Categories (Claude's discretion per D-07):

    **Setup and configuration:**
    | Command | What it does |
    | `/donna:setup` | First-time configuration (storage repo, directories) |
    | `/donna:set-role` | Define your job role, get recurring task suggestions |
    | `/donna:add-tool` | Register an external tool (CLI, REST API, GraphQL API, MCP server) |
    | `/donna:adjust-tool` | Edit a tool's configuration (scope, capabilities, auth, command) |

    **Daily workflow:**
    | Command | What it does |
    | `/donna:begin-the-day` | Morning brief with carry-forward, recurring tasks, tool data |
    | `/donna:add-task` | Capture a task instantly |
    | `/donna:done` | Mark a task complete |
    | `/donna:focus` | Distill today's tasks into a short prioritized focus list |

    **Tool management:**
    | Command | What it does |
    | `/donna:run-tools` | Refresh tool data mid-day |
    | `/donna:relearn-tools` | Update CLI tool knowledge after upgrades |

    **Help and feedback:**
    | Command | What it does |
    | `/donna:help` | Conversational troubleshooting for config, storage, or skill issues |
    | `/donna:contribute-idea` | Submit a feature idea or bug report via GitHub Issues |

    Format each group as:
    ```markdown
    ### Setup and configuration

    | Command | What it does |
    |---------|-------------|
    | ... | ... |
    ```

    Keep the `## All commands` H2 heading. Replace the single table with the 4 sub-sections above.

    **D-08: Document why automated periodic run-tools is not supported.**

    Add a new section after the "Idempotent by design" section and before "All commands". Title: `## Why not automate tool pulls?`

    Content (ref: #23):
    ```markdown
    ## Why not automate tool pulls?

    Donna intentionally does not support automated periodic invocations of `/donna:run-tools` (e.g., via cron or background timers). Each tool pull runs inside an AI coding assistant session, which means:

    - **Cost** -- every invocation consumes API tokens. A cron job running hourly would burn tokens even when you are not working.
    - **Context** -- tool data is most useful when you are actively looking at it. Pulling data while you are away produces stale results by the time you return.
    - **Conflicts** -- background runs could write to your daily file while you are editing it, causing git merge conflicts in your storage repo.

    Instead, run `/donna:run-tools` on demand when you want fresh data, or let `/donna:begin-the-day` handle it as part of your morning routine.
    ```

    Run `npm run lint:fix` after changes (in case biome checks markdown).
  </action>
  <verify>
    <automated>grep -c "### Setup and configuration\|### Daily workflow\|### Tool management\|### Help and feedback" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `### Setup and configuration`
    - README.md contains `### Daily workflow`
    - README.md contains `### Tool management`
    - README.md contains `### Help and feedback`
    - README.md contains `## Why not automate tool pulls?`
    - README.md contains "API tokens" (automation docs content)
    - README.md contains all 12 command names: `/donna:setup`, `/donna:set-role`, `/donna:begin-the-day`, `/donna:add-task`, `/donna:done`, `/donna:add-tool`, `/donna:adjust-tool`, `/donna:run-tools`, `/donna:relearn-tools`, `/donna:help`, `/donna:contribute-idea`, `/donna:focus`
    - README.md still contains `## License` (end of file preserved)
  </acceptance_criteria>
  <done>README has grouped skills in 4 categories and a new section explaining why automated tool pulls are not supported. All 12 commands present, no information lost.</done>
</task>

</tasks>

<verification>
- All 12 commands appear in README.md
- 4 category headings present
- Automation docs section exists with explanation
- File renders correctly as markdown (no broken tables)
</verification>

<success_criteria>
- Skills grouped into 4 logical categories with sub-headings
- New "Why not automate tool pulls?" section explains cost, context, and conflict reasons
- All existing content preserved
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-and-harden-version-check-skip-setup-guard-simplify-adjust-tool-uat-merge-gate-docs-and-readme-improvements-enhance-tool-learning-refactor-skill-bootstrap/06-03-SUMMARY.md`
</output>
