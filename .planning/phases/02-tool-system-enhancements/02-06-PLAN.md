---
phase: 02-tool-system-enhancements
plan: "06"
type: execute
wave: 2
depends_on: ["02-05"]
files_modified:
  - workflows/relearn-tools.md
  - workflows/adjust-tool.md
autonomous: true
requirements: [TOOL-04, TOOL-05, TOOL-07]
gap_closure: true

must_haves:
  truths:
    - "relearn-tools introspects GraphQL tools via schema query and reports new/removed fields"
    - "REST and MCP tools continue to skip re-learning (no introspection available)"
    - "adjust-tool can detect and repair capability format mismatches when tool type is changed or was corrupted"
  artifacts:
    - path: "workflows/relearn-tools.md"
      provides: "GraphQL schema introspection in relearn flow"
    - path: "workflows/adjust-tool.md"
      provides: "Capability format repair step"
  key_links:
    - from: "relearn-tools check-versions step"
      to: "GraphQL introspection query"
      via: "curl POST to base_url with __schema query"
      pattern: "__schema|introspection"
    - from: "adjust-tool apply-change type step"
      to: "capability format validation"
      via: "format check after type change"
      pattern: "format mismatch|repair"
---

<objective>
Close two remaining UAT gaps: (1) relearn-tools should introspect GraphQL APIs to detect schema changes instead of blanket-skipping all non-CLI tools; (2) adjust-tool needs a recovery path for capability format mismatches caused by bad backfill or type changes.

Purpose: Make GraphQL tools first-class in the relearn flow and provide a repair mechanism for corrupted tool configurations.
Output: Updated relearn-tools.md with GraphQL introspection + updated adjust-tool.md with format repair.
</objective>

<execution_context>
@/Users/pingvinen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pingvinen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tool-system-enhancements/02-03-SUMMARY.md
@.planning/phases/02-tool-system-enhancements/02-UAT.md

<interfaces>
<!-- From workflows/relearn-tools.md — current non-CLI handling in check-versions step (lines 99-101): -->
If `<type>` is `rest`, `graphql`, or `mcp`:
  Add to `<unchanged_tools>` — version checking is not applicable for non-CLI tools.
  Continue to next tool.

<!-- From workflows/relearn-tools.md — report-unchanged step (lines 121-124): -->
If `<type>` is `rest`, `graphql`, or `mcp`, print:
  ⊘ <tool_name>: <type> tool — re-learning not applicable (capabilities are user-defined)

<!-- From workflows/adjust-tool.md — type change in apply-change step (lines 170-172): -->
Use AskUserQuestion: `New type for <tool_name>? (cli, rest, graphql, mcp) (current: <type>)`. Store new value.
Warn if changing from cli to api/mcp: `Changing type will require updating capabilities format. Proceed? (yes/no)`.

<!-- From workflows/add-tool.md — GraphQL tool section format in write-tools-md: -->
## <tool_name>
- type: graphql
- base_url: <base_url>
- auth_header: <auth_header>
- auth_secret: <auth_secret>
- scope: <scope or "none">
- learned: <today's date YYYY-MM-DD>
### Capabilities
- <capability_name>: <graphql_query>

<!-- From workflows/add-tool.md — MCP tool section format: -->
## <tool_name>
- type: mcp
- learned: <today's date YYYY-MM-DD>
### Capabilities
- <capability_name>: mcp:<server>/<tool>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GraphQL schema introspection to relearn-tools</name>
  <files>workflows/relearn-tools.md</files>
  <action>
In `workflows/relearn-tools.md`, modify the `check-versions` step to give GraphQL tools an introspection path instead of blanket-skipping them.

**Current logic (lines 99-101):**
```
If `<type>` is `rest`, `graphql`, or `mcp`:
  Add to `<unchanged_tools>` — version checking is not applicable for non-CLI tools.
  Continue to next tool.
```

**Replace with:**
```
If `<type>` is `rest` or `mcp`:
  Add to `<unchanged_tools>` — version checking is not applicable for REST/MCP tools.
  Continue to next tool.

If `<type>` is `graphql`:
  Run a GraphQL introspection query to detect schema changes.

  1. Read `<storage_repo>/donna/secrets.md` with the Read tool. Resolve the `auth_secret` key to get the actual secret value. If missing or placeholder, add to `<unchanged_tools>` with note "skipped — no secret configured" and continue.

  2. Run via Bash with a 15-second timeout:
     ```bash
     timeout 15 curl -s -X POST \
       -H "<auth_header>: <resolved_secret>" \
       -H "Content-Type: application/json" \
       -d '{"query":"{ __schema { types { name fields { name type { name } } } } }"}' \
       "<base_url>" 2>&1
     ```

  3. If the request fails (non-zero exit, timeout, or error response), add to `<unchanged_tools>` with note "introspection failed — skipped" and continue.

  4. If successful, compare the returned schema against stored capabilities:
     - Extract field names and types from the introspection response for the types/queries relevant to stored capabilities.
     - Check if any stored capability references fields that no longer exist in the schema (removed fields).
     - Check if the schema has new fields on types used by stored capabilities that might be useful.

  5. If no meaningful changes detected, add to `<unchanged_tools>`.

  6. If changes detected, add to `<changed_tools>` with a `<schema_changes>` annotation listing:
     - Removed fields (fields in stored capabilities no longer in schema)
     - New fields (fields in schema not referenced by any stored capability)
```

**Update the `report-unchanged` step:**

Change the GraphQL message from:
```
⊘ <tool_name>: <type> tool — re-learning not applicable (capabilities are user-defined)
```
To show the skip reason for GraphQL tools:
```
⊘ <tool_name>: graphql tool — <skip_reason>
```
Where `<skip_reason>` is "no schema changes detected", "skipped — no secret configured", or "introspection failed — skipped".

Keep the existing message for REST and MCP tools: `re-learning not applicable (capabilities are user-defined)`.

**Add a new step `relearn-graphql` after `report-unchanged` and before `relearn-changed`:**

```
For each graphql tool in `<changed_tools>`:

Print:
```
⚠ <tool_name>: schema changes detected
  Removed fields: <list or "none">
  New fields: <list or "none">
```

Use AskUserQuestion:
```
Update capabilities for <tool_name>? (yes to review changes, no to skip)
```

If yes:
  Show the current capabilities and the detected changes side by side. Use AskUserQuestion to let the user update capabilities interactively (same editing loop as adjust-tool: "remove <number>", "add <name>: <query>", "edit <number> <new_query>", "done").

  Store the updated capabilities. Update `learned` date to today.

If no:
  Skip — keep existing capabilities unchanged. Update `learned` date to today (to avoid re-checking next run).
```

**Update the `relearn-changed` step note:**

Change from:
```
Note: Re-learning is currently supported for CLI tools only. REST, GraphQL, and MCP tool capabilities are user-defined and not auto-learned.
```
To:
```
Note: Re-learning is supported for CLI tools (version-based) and GraphQL tools (schema introspection). REST and MCP tool capabilities are user-defined and not auto-learned.
```
  </action>
  <verify>
    <automated>grep -c "__schema\|introspection\|schema changes" workflows/relearn-tools.md | grep -v ":0" && grep "REST and MCP" workflows/relearn-tools.md</automated>
  </verify>
  <done>GraphQL tools get schema introspection during relearn-tools. Schema changes are reported to the user with an interactive update option. REST and MCP tools continue to skip. The relearn-changed note reflects the new GraphQL support.</done>
</task>

<task type="auto">
  <name>Task 2: Add capability format repair to adjust-tool</name>
  <files>workflows/adjust-tool.md</files>
  <action>
In `workflows/adjust-tool.md`, enhance the `apply-change` step's **type change** handler (option 5) to include automatic capability format repair. This addresses the scenario where a tool's type was corrupted by the old backfill (e.g., MCP tool stamped as CLI) and the user fixes the type but capabilities remain in the wrong format.

**Current type change logic (lines 170-172):**
```
Use AskUserQuestion: `New type for <tool_name>? (cli, rest, graphql, mcp) (current: <type>)`. Store new value.
Warn if changing from cli to api/mcp: `Changing type will require updating capabilities format. Proceed? (yes/no)`.
```

**Replace with:**
```
Use AskUserQuestion:
```
What is the correct type for <tool_name>? (cli, rest, graphql, mcp) Current: <type>
```

Store new value as `<new_type>`.

If `<new_type>` differs from `<type>`, check for capability format mismatches:

**Detect format mismatches:**
For each capability line (`- <name>: <invocation>`), check if the invocation matches the expected format for `<new_type>`:
- `cli`: invocation should be a shell command (no `mcp:` prefix, no `GET /` or `POST /` pattern, no `query {` pattern)
- `rest`: invocation should match `<METHOD> /path` pattern (e.g., `GET /repos/...`)
- `graphql`: invocation should contain `query {` or `mutation {`
- `mcp`: invocation should match `mcp:<server>/<tool>` pattern

If ANY capability does not match the expected format for `<new_type>`, print:

```
⚠ Capability format mismatch detected. Current capabilities are in <old_type> format but type is changing to <new_type>.

Mismatched capabilities:
<list each mismatched capability with its current invocation>
```

Use AskUserQuestion:
```
How would you like to fix the capabilities?
1. Re-enter capabilities manually (interactive editor)
2. Clear all capabilities (start fresh with adjust-tool later)
3. Keep as-is (may cause runtime errors)
```

If option 1: Present the same interactive capabilities editing loop used elsewhere (show numbered list, accept "remove N", "add name: invocation", "edit N new_invocation", "done"). Pre-populate with existing capability NAMES but empty invocations so the user only needs to type the new format.

If option 2: Clear the capabilities section entirely. Print `✓ Capabilities cleared. Run /donna:adjust-tool <tool_name> to add new capabilities.`

If option 3: Print `⚠ Keeping mismatched capabilities — runtime errors may occur.`

Also update the tool section's structural fields when type changes:
- Changing TO rest/graphql: ensure `base_url`, `auth_header`, `auth_secret` fields exist. If missing, prompt for them (same prompts as add-tool).
- Changing TO mcp: remove `command`, `version`, `base_url`, `auth_header`, `auth_secret` fields if present.
- Changing TO cli: ensure `command`, `version` fields exist. If missing, prompt for command and run version check.
- Changing FROM rest/graphql: remove `base_url`, `auth_header`, `auth_secret` fields.
```
  </action>
  <verify>
    <automated>grep -c "format mismatch\|Mismatched capabilities\|Re-enter capabilities" workflows/adjust-tool.md | grep -v ":0"</automated>
  </verify>
  <done>adjust-tool detects capability format mismatches when type is changed, offers three repair options (re-enter, clear, keep), and handles structural field changes between types. An MCP tool that was incorrectly backfilled as CLI can be repaired by changing type to mcp and re-entering capabilities in mcp:server/tool format.</done>
</task>

</tasks>

<verification>
1. relearn-tools.md contains GraphQL introspection query with __schema
2. relearn-tools.md still skips REST and MCP tools (not applicable)
3. adjust-tool.md detects capability format mismatches on type change
4. adjust-tool.md offers repair options including interactive re-entry
5. `npm run lint:fix` passes
</verification>

<success_criteria>
- A GraphQL tool with a valid secret triggers schema introspection during relearn-tools and reports any field changes
- A GraphQL tool with no secret is gracefully skipped with a clear message
- Changing an MCP tool's type from cli to mcp in adjust-tool detects that capabilities are in CLI format and offers repair
- After repair, the MCP tool's capabilities are in `mcp:<server>/<tool>` format and run-tools can execute them
</success_criteria>

<output>
After completion, create `.planning/phases/02-tool-system-enhancements/02-06-SUMMARY.md`
</output>
