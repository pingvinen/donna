---
phase: 05-fix-the-constant-timeout-warnings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/begin-the-day.md
  - workflows/run-tools.md
  - workflows/focus.md
  - workflows/add-tool.md
  - workflows/relearn-tools.md
  - test/stubs.test.cjs
autonomous: true
requirements: []

must_haves:
  truths:
    - "No workflow file contains the string 'timeout N' as a bash command prefix"
    - "All workflows still instruct Claude to use a timeout when running tool commands"
    - "Timeout durations are preserved (10s for tool commands, 15s for GraphQL introspection)"
    - "All existing tests pass after the changes"
  artifacts:
    - path: "workflows/begin-the-day.md"
      provides: "Tool execution instructions without timeout binary"
      contains: "10000"
    - path: "workflows/run-tools.md"
      provides: "Tool execution instructions without timeout binary"
      contains: "10000"
    - path: "workflows/focus.md"
      provides: "Tool execution instructions without timeout binary"
      contains: "10000"
    - path: "workflows/add-tool.md"
      provides: "Auth test instructions without timeout binary"
      contains: "10000"
    - path: "workflows/relearn-tools.md"
      provides: "GraphQL introspection instructions without timeout binary"
      contains: "15000"
    - path: "test/stubs.test.cjs"
      provides: "Updated assertions checking for Bash tool timeout parameter"
      contains: "10000"
  key_links:
    - from: "test/stubs.test.cjs"
      to: "workflows/run-tools.md"
      via: "content.includes assertion"
      pattern: "includes.*10000"
    - from: "test/stubs.test.cjs"
      to: "workflows/begin-the-day.md"
      via: "content.includes assertion"
      pattern: "includes.*10000"
---

<objective>
Remove all `timeout N` binary invocations from Donna workflow markdown files and replace them with prose instructing Claude to use the Bash tool's native `timeout` parameter (in milliseconds). Update test assertions to verify the new pattern.

Purpose: The `timeout` binary is not available on macOS without coreutils, causing constant warnings when Donna runs. The Bash tool Claude Code uses already has a native `timeout` parameter that works cross-platform.
Output: 5 updated workflow files + 1 updated test file, all 287+ tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-fix-the-constant-timeout-warnings/05-CONTEXT.md
@.planning/phases/05-fix-the-constant-timeout-warnings/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove timeout binary from all 5 workflow files</name>
  <files>workflows/begin-the-day.md, workflows/run-tools.md, workflows/focus.md, workflows/add-tool.md, workflows/relearn-tools.md</files>
  <read_first>
    workflows/begin-the-day.md
    workflows/run-tools.md
    workflows/focus.md
    workflows/add-tool.md
    workflows/relearn-tools.md
    .planning/phases/05-fix-the-constant-timeout-warnings/05-CONTEXT.md
  </read_first>
  <action>
Replace every `timeout N` binary invocation across all 5 workflow files with prose instructing Claude to use the Bash tool's native `timeout` parameter. Per D-01, D-02, D-03 — same durations, no external binary.

**Replacement pattern for 10-second timeouts (cli/rest/graphql in begin-the-day, run-tools, focus, add-tool):**

Where a fenced bash block currently contains `timeout 10 <command> 2>&1`, change the block to remove the `timeout 10` prefix. The surrounding prose line that says "Run via Bash with a 10-second timeout:" should be updated to:

```
Run via Bash (set the Bash tool's `timeout` parameter to `10000`):
```

And the fenced bash block becomes just:
```bash
<command> 2>&1
```

**Replacement pattern for 15-second timeouts (relearn-tools.md GraphQL introspection):**

Same approach but with `15000` instead of `10000`. Change prose to:

```
Run via Bash with a 15-second timeout (set the Bash tool's `timeout` parameter to `15000`). Include the auth header only when a real secret was resolved:
```

And remove `timeout 15` prefix from the two fenced bash blocks.

**Specific file changes:**

**begin-the-day.md** (3 fenced blocks):
- Line 181: Change prose to "Run via Bash (set the Bash tool's `timeout` parameter to `10000`):"
- Line 183: Change `timeout 10 <cli_invocation> 2>&1` to `<cli_invocation> 2>&1`
- Line 213: Change prose to include `(set the Bash tool's \`timeout\` parameter to \`10000\`)`
- Line 215: Change `timeout 10 curl -s -H ...` to `curl -s -H ...`
- Line 226: Change prose to include `(set the Bash tool's \`timeout\` parameter to \`10000\`)`
- Line 228: Change `timeout 10 curl -s -X POST ...` to `curl -s -X POST ...`

**run-tools.md** (4 occurrences):
- Line 146: Change prose to "Run the capability command via Bash (set the Bash tool's `timeout` parameter to `10000`):"
- Line 148: Change `timeout 10 <capability_command> 2>&1` to `<capability_command> 2>&1`
- Line 179: Change prose to include `(set the Bash tool's \`timeout\` parameter to \`10000\`)`
- Line 181: Change `timeout 10 curl -s -H ...` to `curl -s -H ...`
- Line 192: Change prose to include `(set the Bash tool's \`timeout\` parameter to \`10000\`)`
- Line 194: Change `timeout 10 curl -s -X POST ...` to `curl -s -X POST ...`
- Line 226 (inline prose): Change `timeout 10 gh pr view <number> --json state --jq '.state' 2>/dev/null || timeout 10 gh issue view <number> --json state --jq '.state' 2>/dev/null` to `gh pr view <number> --json state --jq '.state' 2>/dev/null || gh issue view <number> --json state --jq '.state' 2>/dev/null` and add note to use Bash tool with `timeout: 10000`

**focus.md** (3 fenced blocks):
- Line 156-158: Add prose "Run via Bash (set the Bash tool's `timeout` parameter to `10000`):" before the block, change `timeout 10 <cli_invocation> 2>&1` to `<cli_invocation> 2>&1`
- Line 164: Change prose to include `(set the Bash tool's \`timeout\` parameter to \`10000\`)`
- Line 166: Change `timeout 10 curl -s -H ...` to `curl -s -H ...`
- Line 171: Change prose to include `(set the Bash tool's \`timeout\` parameter to \`10000\`)`
- Line 173: Change `timeout 10 curl -s -X POST ...` to `curl -s -X POST ...`

**add-tool.md** (3 inline backtick references):
- Line 166: Keep prose "For well-known tools, run the appropriate auth test via Bash with a 10-second timeout:" and update to "For well-known tools, run the appropriate auth test via Bash (set the Bash tool's `timeout` parameter to `10000`):"
- Line 168: Change `` `timeout 10 gh api user --jq '.login' 2>&1` `` to `` run `gh api user --jq '.login' 2>&1` via Bash with `timeout: 10000` ``
- Line 169: Change `` `timeout 10 jira me 2>&1` `` to `` run `jira me 2>&1` via Bash with `timeout: 10000` ``
- Line 170: Change `` `timeout 10 kubectl auth whoami 2>&1` `` to `` run `kubectl auth whoami 2>&1` via Bash with `timeout: 10000` ``

**relearn-tools.md** (2 fenced blocks):
- Line 119: Change prose to "Run via Bash with a 15-second timeout (set the Bash tool's `timeout` parameter to `15000`). Include the auth header only when a real secret was resolved:"
- Line 122: Change `timeout 15 curl -s -X POST \` to `curl -s -X POST \`
- Line 130: Change `timeout 15 curl -s -X POST \` to `curl -s -X POST \`

**IMPORTANT — Do NOT change:**
- Display text like "timed out after 10s" in error message templates (line 203 of begin-the-day.md, etc.)
- "timed out (2-minute batch limit)" global timeout prose
- "timeout" when used as a noun in failure condition descriptions like "non-zero exit, timeout, missing secret"
- The word "timeout" on line 234 of add-tool.md: "Other/timeout: print..."
- Any error handling prose that mentions timeout as a failure condition

**After editing all files, verify zero remaining `timeout [0-9]` patterns:**
Run `grep -n "timeout [0-9]" workflows/begin-the-day.md workflows/run-tools.md workflows/focus.md workflows/add-tool.md workflows/relearn-tools.md` — must return empty.
  </action>
  <verify>
    <automated>grep -rn "timeout [0-9]" workflows/begin-the-day.md workflows/run-tools.md workflows/focus.md workflows/add-tool.md workflows/relearn-tools.md | grep -v "^$" | wc -l | tr -d ' ' | grep -x "0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "timeout [0-9]" workflows/begin-the-day.md` returns 0
    - `grep -c "timeout [0-9]" workflows/run-tools.md` returns 0
    - `grep -c "timeout [0-9]" workflows/focus.md` returns 0
    - `grep -c "timeout [0-9]" workflows/add-tool.md` returns 0
    - `grep -c "timeout [0-9]" workflows/relearn-tools.md` returns 0
    - `grep -c "10000" workflows/begin-the-day.md` returns at least 3 (cli, rest, graphql)
    - `grep -c "10000" workflows/run-tools.md` returns at least 4 (cli, rest, graphql, smart-merge gh check)
    - `grep -c "10000" workflows/focus.md` returns at least 3
    - `grep -c "10000" workflows/add-tool.md` returns at least 1
    - `grep -c "15000" workflows/relearn-tools.md` returns at least 1
    - `grep -c "timed out after 10s" workflows/begin-the-day.md` returns at least 1 (error template preserved)
  </acceptance_criteria>
  <done>All 15 `timeout N` binary invocations removed from workflow files. Each replaced with Bash tool `timeout` parameter prose using correct millisecond values (10000 or 15000). Error message templates and failure condition text unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update test assertions and verify full suite passes</name>
  <files>test/stubs.test.cjs</files>
  <read_first>
    test/stubs.test.cjs
    workflows/run-tools.md
    workflows/begin-the-day.md
  </read_first>
  <action>
Per D-04, update the 2 test assertions in `test/stubs.test.cjs` that check for the `timeout` binary keyword in workflow content.

**Assertion 1 — lines 822-828** (describe block "workflow: run-tools.md"):

Replace:
```javascript
it("contains timeout for failure isolation", () => {
    const content = fs.readFileSync(runToolsWorkflowPath, "utf8");
    assert.ok(
        content.includes("timeout"),
        "Should contain timeout for per-tool failure isolation",
    );
});
```

With:
```javascript
it("contains Bash tool timeout parameter for failure isolation", () => {
    const content = fs.readFileSync(runToolsWorkflowPath, "utf8");
    assert.ok(
        content.includes("10000"),
        "Should use Bash tool timeout parameter (10000ms) for per-tool failure isolation",
    );
});
```

**Assertion 2 — lines 894-900** (describe block "cross-cutting: begin-the-day tool integration"):

Replace:
```javascript
it("includes timeout for failure isolation", () => {
    const content = fs.readFileSync(beginTheDayWorkflowPath, "utf8");
    assert.ok(
        content.includes("timeout 10") || content.includes("timeout"),
        "begin-the-day should use timeout for tool command failure isolation",
    );
});
```

With:
```javascript
it("includes Bash tool timeout parameter for failure isolation", () => {
    const content = fs.readFileSync(beginTheDayWorkflowPath, "utf8");
    assert.ok(
        content.includes("10000"),
        "begin-the-day should use Bash tool timeout parameter (10000ms) for tool command failure isolation",
    );
});
```

After updating assertions, run the full test suite to confirm all tests pass.
  </action>
  <verify>
    <automated>cd /Users/pingvinen/workspace/github/donna && npm run lint:fix && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "10000" test/stubs.test.cjs` returns at least 2 (one per updated assertion)
    - `grep -c 'timeout 10' test/stubs.test.cjs` returns 0 (old assertions removed)
    - `npm test` exits 0 with all tests passing (287+ tests)
  </acceptance_criteria>
  <done>Both test assertions updated to check for "10000" (Bash tool timeout parameter) instead of "timeout" binary keyword. Full test suite passes.</done>
</task>

</tasks>

<verification>
1. `grep -rn "timeout [0-9]" workflows/*.md` returns zero matches — no `timeout N` binary prefix remains in any workflow
2. `grep -c "10000" workflows/begin-the-day.md workflows/run-tools.md workflows/focus.md workflows/add-tool.md` — each file has at least 1 match
3. `grep -c "15000" workflows/relearn-tools.md` — at least 1 match
4. `npm test` — all tests pass (287+)
5. `grep "timed out after 10s" workflows/begin-the-day.md` — error message template preserved
</verification>

<success_criteria>
- Zero `timeout N` binary invocations remain in any workflow file
- All 5 workflows instruct Claude to use the Bash tool's `timeout` parameter with correct millisecond values
- Both test assertions verify the new "10000" pattern
- Full test suite (287+ tests) passes
- Error message templates and failure condition prose unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/05-fix-the-constant-timeout-warnings/05-01-SUMMARY.md`
</output>
