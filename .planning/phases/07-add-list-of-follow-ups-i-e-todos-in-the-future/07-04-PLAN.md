---
phase: 07-add-list-of-follow-ups-i-e-todos-in-the-future
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/follow-up.md
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "Interactive follow-up prompts are clear and usable"
    - "Invalid date expressions show an error to the user"
  artifacts:
    - path: "workflows/follow-up.md"
      provides: "Follow-up capture workflow with clear prompts and error handling"
  key_links:
    - from: "workflows/follow-up.md parse-input step"
      to: "AsUserQuestion invocation"
      via: "Free-text questions without parenthetical examples — examples printed as prose before ask"
      pattern: "AskUserQuestion"
    - from: "workflows/follow-up.md resolve-date step"
      to: "invalid date detection"
      via: "Error message + halt instead of silent fallback to today"
      pattern: "invalid"
---

<objective>
Fix two major UAT gaps in the follow-up workflow: (1) confusing interactive prompts that trigger picker mode due to parenthetical examples inside AskUserQuestion text, and (2) invalid dates silently falling back to today instead of producing an error.

Purpose: Resolve the two blocking UX issues reported in UAT tests 2 and 4.
Output: A corrected `workflows/follow-up.md` where interactive prompts are single-sentence questions (examples printed as prose) and invalid dates produce an error + halt.
</objective>

<execution_context>
@$HOME/.config/kilo/gsd-core/workflows/execute-plan.md
@$HOME/.config/kilo/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-add-list-of-follow-ups-i-e-todos-in-the-future/07-01-SUMMARY.md
@.planning/phases/07-add-list-of-follow-ups-i-e-todos-in-the-future/07-UAT.md

Existing patterns:
- `stubs/claude-code/donna/add-task.md` — stub with single-sentence AskUserQuestion (no parenthetical examples)
- `workflows/follow-up.md` — current state, needs fixes at parse-input (lines 44-57) and resolve-date (line 110)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix interactive prompt text to avoid picker mode</name>
  <files>workflows/follow-up.md</files>
  <action>
In the parse-input step (lines 42-57), fix both AskUserQuestion prompts so they match the add-task.md pattern: print time-expression examples as prose BEFORE the question, then ask with a single-sentence question text.

Specifically:
1. In the "If no argument was provided" block (line 42), before the first AskUserQuestion, add a prose block:
   ```
   Time expression examples: "in 2 months", "on 2026-09-15", "next Tuesday", "in 3 weeks", or leave blank for today.
   ```
2. Replace the first AskUserQuestion (lines 45-49) — the question text is fine ("What task would you like to schedule?"), so leave it as-is. But remove the "CRITICAL: Use free-text input mode..." line at line 57 (it becomes the global rule at step level).
3. Replace the second AskUserQuestion text (lines 52-55) from:
   ```
   When is it due? (e.g. "in 2 months", "on 2026-09-15", or leave blank for today)
   ```
   to a simple single-sentence question:
   ```
   When is it due?
   ```
   The parenthetical examples that trigger Claude Code's picker mode are removed. The user already saw the examples in the prose block printed before the first question.

After both questions, keep the instruction: "Store the free-text response as `<due_expression>`. If the user leaves it blank or says "today", set `<due_expression>` to null."

Add a CRITICAL note at the top of the "If no argument was provided" block: "CRITICAL: Both AskUserQuestion prompts must use free-text input mode — do NOT use a picker with predefined options."

Gap reference: UAT test 2 — AskUserQuestion parenthetical examples trigger picker mode, making the UI confusing. Root cause in UAT.md lines 79-85.
</action>
  <verify>
    <automated>grep -c "e.g." workflows/follow-up.md | xargs test 0 -eq</automated>
  </verify>
  <done>Both AskUserQuestion prompts in the follow-up workflow are single-sentence questions. Time-expression examples appear in prose printed before the questions, not inside AskUserQuestion text. The "When is it due?" prompt does not contain parenthetical examples.</done>
</task>

<task type="auto">
  <name>Task 2: Replace silent date fallback with error + halt</name>
  <files>workflows/follow-up.md</files>
  <action>
In the resolve-date step, replace the fallback instruction at line 110:

Current text (line 110):
```
If the resolved date is NaN or invalid (check by verifying the output matches YYYY-MM-DD format), fall back to today's date (re-run Case 1).
```

Replace with:
```
If the resolved date is NaN or invalid (check by verifying the output matches YYYY-MM-DD format), print:
```
x Invalid date expression: <due_expression>. Use formats like "tomorrow", "next friday", "in 3 days", or YYYY-MM-DD.
```
Then stop the workflow. Do NOT fall back to today — the user must correct their date expression.

If the date resolved successfully, store the output as `<due_date>`.
```

The `Store the output as <due_date>.` line (currently line 112) moves into the success path. The invalid path halts.

Gap reference: UAT test 4 — user reported that silently falling back is dangerous because the user thinks their date was accepted. Root cause in UAT.md lines 92-98.
</action>
  <verify>
    <automated>bash -c "rg -c 'fall back' workflows/follow-up.md | grep -v '^#' | grep -c 'fall back' | xargs test 0 -eq"</automated>
  </verify>
  <done>"fall back" no longer appears in the resolve-date step. An invalid date expression prints an error message showing the rejected input and valid format examples, then halts. Valid dates are stored as `<due_date>`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → workflow parse-input | Untrusted free-text task description and date expression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | resolve-date step | mitigate | Invalid date expressions produce error + halt (no silent fallback) |
| T-07-02 | Information Disclosure | parse-input step | accept | Description is plain text markdown; no sensitive data paths |
</threat_model>

<verification>
- `grep -c "e.g." workflows/follow-up.md` → 0 (no parenthetical examples in AskUserQuestion text)
- grep for "fall back" in resolve-date step → no matches (invalid dates halt, not fall back)
- `grep -q '<\!-- gsd:write-continue -->'` not present (not a fragmented file)
</verification>

<success_criteria>
1. Interactive follow-up prompting uses single-sentence AskUserQuestion text with examples printed as prose — matching add-task.md pattern
2. Invalid date expressions show an error message with valid format examples and halt the workflow
3. Valid date expressions continue to resolve correctly (today, YYYY-MM-DD, relative expressions)
</success_criteria>

<output>
Create `.planning/phases/07-add-list-of-follow-ups-i-e-todos-in-the-future/07-04-SUMMARY.md` when done
</output>