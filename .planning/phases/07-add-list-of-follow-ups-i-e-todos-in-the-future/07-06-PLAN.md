---
phase: 07-add-list-of-follow-ups-i-e-todos-in-the-future
plan: "06"
type: execute
wave: 2
depends_on:
  - "07-04"
  - "07-05"
files_modified:
  - stubs/claude-code/donna/follow-up.md
  - workflows/follow-up.md
  - src/installer.cjs
  - README.md
  - test/stubs.test.cjs
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "Skill is named for discoverability alongside add-task — typing `/add` surfaces both `add-task` and `add-follow-up-task`"
  artifacts:
    - path: "stubs/claude-code/donna/add-follow-up-task.md"
      provides: "Renamed stub with donna:add-follow-up-task frontmatter name"
    - path: "workflows/follow-up.md"
      provides: "Updated inline examples referencing /donna:add-follow-up-task"
    - path: "src/installer.cjs"
      provides: "Installer success message listing add-follow-up-task"
    - path: "README.md"
      provides: "Command table entry for /donna:add-follow-up-task"
    - path: "test/stubs.test.cjs"
      provides: "Updated test assertions for renamed stub and workflow"
  key_links:
    - from: "stubs/claude-code/donna/add-follow-up-task.md"
      to: "workflows/follow-up.md"
      via: "@~/.donna/workflows/follow-up.md reference in execution_context"
      pattern: "@~\\.donna/workflows/follow-up\\.md"
    - from: "workflows/follow-up.md parse-input step"
      to: "stubs/claude-code/donna/add-follow-up-task.md"
      via: "Inline examples showing /donna:add-follow-up-task"
      pattern: "donna:add-follow-up-task"
---

<objective>
Rename the follow-up skill from `donna:follow-up` to `donna:add-follow-up-task` for better discoverability — typing `/add` in Claude Code surfaces both `add-task` and `add-follow-up-task`. This requires renaming the stub file, updating its frontmatter name, and propagating the new name through the workflow, installer, README, and test suite.

Purpose: User testing found that `donna:follow-up` doesn't surface alongside `donna:add-task` when the user types `/add`. Renaming to `donna:add-follow-up-task` makes it discoverable.
Output: Renamed stub file, updated frontmatter, and 5 source files with all references updated.
</objective>

<execution_context>
@$HOME/.config/kilo/gsd-core/workflows/execute-plan.md
@$HOME/.config/kilo/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-add-list-of-follow-ups-i-e-todos-in-the-future/07-01-SUMMARY.md
@.planning/phases/07-add-list-of-follow-ups-i-e-todos-in-the-future/07-03-SUMMARY.md
@.planning/phases/07-add-list-of-follow-ups-i-e-todos-in-the-future/07-UAT.md

Existing patterns:
- `stubs/claude-code/donna/add-task.md` — sibling stub with name `donna:add-task`, discoverable via `/add`
- `workflows/follow-up.md` — will be modified by plan 07-04 (depends_on ensures those changes land first)
- `test/stubs.test.cjs` — will be modified by plan 07-05 (depends_on ensures those changes land first)

Current state (pre-rename):
- Stub file: `stubs/claude-code/donna/follow-up.md`, frontmatter `name: donna:follow-up`
- Workflow inline examples: `/donna:follow-up` (lines 29, 40)
- Installer: `follow-up` in success message (line 82)
- README: `/donna:follow-up` in command table (line 156), `follow-ups.md` in directory tree (line 114)
- Tests: 14 assertions reference `donna:follow-up` or `follow-up.md` paths
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename stub file and update frontmatter name</name>
  <files>stubs/claude-code/donna/follow-up.md</files>
  <action>
Rename the stub file from `stubs/claude-code/donna/follow-up.md` to `stubs/claude-code/donna/add-follow-up-task.md` and update the frontmatter name.

Steps:
1. Use Bash to rename the file:
   ```bash
   mv stubs/claude-code/donna/follow-up.md stubs/claude-code/donna/add-follow-up-task.md
   ```
2. Read the renamed file `stubs/claude-code/donna/add-follow-up-task.md`
3. Change the frontmatter `name` field from `donna:follow-up` to `donna:add-follow-up-task`
4. Do NOT change the description field — "Schedule a follow-up task for a future date" is still accurate
5. Do NOT change the execution_context reference — it still points to `@~/.donna/workflows/follow-up.md` (the workflow file is NOT being renamed, only the stub name changes)

The stub now follows the `add-task.md` naming convention: `donna:add-follow-up-task` surfaces alongside `donna:add-task` when the user types `/add`.

Gap reference: UAT test 10 — rename for discoverability. Root cause in UAT.md lines 123-141.
</action>
  <verify>
    <automated>bash -c "test -f stubs/claude-code/donna/add-follow-up-task.md && test ! -f stubs/claude-code/donna/follow-up.md && grep -q 'name: donna:add-follow-up-task' stubs/claude-code/donna/add-follow-up-task.md"</automated>
  </verify>
  <done>The stub file exists at `stubs/claude-code/donna/add-follow-up-task.md` (old path deleted), frontmatter `name` is `donna:add-follow-up-task`, execution_context still references `@~/.donna/workflows/follow-up.md`.</done>
</task>

<task type="auto">
  <name>Task 2: Update workflow inline examples to new skill name</name>
  <files>workflows/follow-up.md</files>
  <action>
In `workflows/follow-up.md`, update the inline command examples from `/donna:follow-up` to `/donna:add-follow-up-task`.

Two locations to update:

1. **Line 29** — the parse-input step argument description:
   Change:
   ```
   The argument is the full user text (e.g., "/donna:follow-up remind team about Q3 planning in 2 months").
   ```
   To:
   ```
   The argument is the full user text (e.g., "/donna:add-follow-up-task remind team about Q3 planning in 2 months").
   ```

2. **Line 40** — the parse-input examples block, last example:
   Change:
   ```
   - "/donna:follow-up schedule 1:1 with Sarah next Tuesday at 2pm" → description: "schedule 1:1 with Sarah", due_expression: "next Tuesday"
   ```
   To:
   ```
   - "/donna:add-follow-up-task schedule 1:1 with Sarah next Tuesday at 2pm" → description: "schedule 1:1 with Sarah", due_expression: "next Tuesday"
   ```

These are the only two locations in workflows/follow-up.md that reference the skill name inline. The git-commit step uses `donna(follow-up)` as the commit prefix — leave that unchanged (it describes the workflow, not the slash command).

Gap reference: UAT test 10 — update workflow inline examples. Root cause in UAT.md line 128.
</action>
  <verify>
    <automated>bash -c "grep -c '/donna:follow-up' workflows/follow-up.md | xargs test 0 -eq"</automated>
  </verify>
  <done>No references to `/donna:follow-up` remain in `workflows/follow-up.md`. Both inline examples now use `/donna:add-follow-up-task`.</done>
</task>

<task type="auto">
  <name>Task 3: Update installer, README, and tests for renamed skill</name>
  <files>src/installer.cjs, README.md, test/stubs.test.cjs</files>
  <action>
Propagate the skill rename through the installer, README, and test suite:

**1. src/installer.cjs (line 82):**
Change `follow-up` to `add-follow-up-task` in the skills list string:
```
Copied donna skills (setup, add-task, done, set-role, begin-the-day, add-tool, remove-tool, relearn-tools, run-tools, help, contribute-idea, adjust-tool, focus, add-follow-up-task) to ...
```

**2. README.md (line 156):**
Change the command table row from `/donna:follow-up` to `/donna:add-follow-up-task`:
```
| `/donna:add-follow-up-task` | Schedule a task for a future date — Donna reminds you when it is due |
```

Note: `follow-ups.md` in the directory tree (line 114) stays unchanged — the storage file name does not change.

**3. test/stubs.test.cjs — 14 changes across 5 groups:**

Group A — Path variables (lines 41-42):
Change:
```javascript
const followUpStubPath = path.join(projectRoot, "stubs", "claude-code", "donna", "follow-up.md");
const followUpWorkflowPath = path.join(projectRoot, "workflows", "follow-up.md");
```
To:
```javascript
const followUpStubPath = path.join(projectRoot, "stubs", "claude-code", "donna", "add-follow-up-task.md");
const followUpWorkflowPath = path.join(projectRoot, "workflows", "follow-up.md");
```
The workflow path does NOT change — only the stub file was renamed.

Group B — Stub describe block (line 1404):
Change from `describe("stub: stubs/claude-code/donna/follow-up.md", ...` to `describe("stub: stubs/claude-code/donna/add-follow-up-task.md", ...`

Group C — Stub name assertions (lines 1409-1414):
Change both the test name and assertion from `donna:follow-up` to `donna:add-follow-up-task`:
```javascript
it('has YAML frontmatter with name "donna:add-follow-up-task"', () => {
    const content = fs.readFileSync(followUpStubPath, "utf8");
    assert.ok(content.startsWith("---"), "Should start with YAML frontmatter delimiter");
    assert.ok(
        content.includes("name: donna:add-follow-up-task"),
        "Should have name: donna:add-follow-up-task in frontmatter",
    );
});
```

Group D — Stub workflow reference (lines 1446-1452):
The execution_context reference to `@~/.donna/workflows/follow-up.md` does NOT change (workflow file not renamed). The test assertion at line 1449 already checks for `@~/.donna/workflows/follow-up.md` — leave it as-is.

Group E — Installer cross-cutting test (lines 1244-1250):
Change from checking for `follow-up` to checking for `add-follow-up-task`:
```javascript
it('success message includes "add-follow-up-task"', () => {
    const content = fs.readFileSync(installerPath, "utf8");
    assert.ok(
        content.includes("add-follow-up-task"),
        "Installer success message should include add-follow-up-task skill",
    );
});
```

Note: The workflow describe block (line 1455), begin-the-day integration tests (lines 1487-1520), and all other tests that reference `donna/follow-ups.md` (the storage file) do NOT change — only the stub file and skill name are renamed. The workflow file, storage file, and their references stay the same.

Gap reference: UAT test 10 — rename skill across all files. Root cause in UAT.md lines 125-141.
</action>
  <verify>
    <automated>node --test test/stubs.test.cjs --test-name-pattern="follow-up|add-follow-up-task"</automated>
  </verify>
  <done>All 14 test assertions pass with the renamed skill. `donna:follow-up` no longer appears in any source file (stub, workflow examples, installer, README, tests). The skill is now registered as `donna:add-follow-up-task` and discoverable alongside `donna:add-task`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — this plan renames identifiers, does not change logic or data flow.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | stub rename | accept | Rename is mechanical — old file deleted, new file created with updated frontmatter. No logic changes. Tests verify correctness. |
</threat_model>

<verification>
- `test -f stubs/claude-code/donna/add-follow-up-task.md && test ! -f stubs/claude-code/donna/follow-up.md` → stub renamed
- `grep -r "donna:follow-up" stubs/claude-code/donna/ src/installer.cjs README.md test/stubs.test.cjs workflows/follow-up.md` → 0 matches (old name fully purged from source files — the only remaining reference may be in commit history or UAT docs, which are not source)
- `grep "donna:add-follow-up-task" stubs/claude-code/donna/add-follow-up-task.md src/installer.cjs README.md | wc -l` → ≥ 2 (present in stub and README)
- `node --test test/stubs.test.cjs` → all follow-up-related tests pass
</verification>

<success_criteria>
1. Stub file renamed from `follow-up.md` to `add-follow-up-task.md` with frontmatter `name: donna:add-follow-up-task`
2. Workflow inline examples updated from `/donna:follow-up` to `/donna:add-follow-up-task`
3. Installer success message lists `add-follow-up-task`
4. README command table shows `/donna:add-follow-up-task`
5. All 14 test assertions pass with the new name
6. Old name `/donna:follow-up` / `donna:follow-up` no longer appears in any source file
</success_criteria>

<output>
Create `.planning/phases/07-add-list-of-follow-ups-i-e-todos-in-the-future/07-06-SUMMARY.md` when done
</output>