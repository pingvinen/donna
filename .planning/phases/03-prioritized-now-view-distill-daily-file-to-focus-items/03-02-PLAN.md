---
phase: 03-prioritized-now-view-distill-daily-file-to-focus-items
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - test/stubs.test.cjs
autonomous: true
requirements:
  - FOCUS-11

must_haves:
  truths:
    - "Test suite includes describe blocks for focus stub and workflow"
    - "All tests pass including new focus tests"
    - "Linting passes clean"
  artifacts:
    - path: "test/stubs.test.cjs"
      provides: "Test blocks for focus stub existence, frontmatter, workflow reference, workflow steps"
      contains: "donna:focus"
  key_links:
    - from: "test/stubs.test.cjs"
      to: "stubs/claude-code/donna/focus.md"
      via: "fs.existsSync and fs.readFileSync assertions"
      pattern: "focusStubPath"
    - from: "test/stubs.test.cjs"
      to: "workflows/focus.md"
      via: "fs.existsSync and fs.readFileSync assertions"
      pattern: "focusWorkflowPath"
---

<objective>
Add test coverage for the donna:focus stub and workflow, then verify the full test suite and linting pass.

Purpose: Ensures the new skill files are structurally correct and match the established patterns. Catches installer string omissions and missing workflow steps.

Output: Updated test file with focus test blocks, green test suite, clean lint.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-prioritized-now-view-distill-daily-file-to-focus-items/03-CONTEXT.md
@.planning/phases/03-prioritized-now-view-distill-daily-file-to-focus-items/03-RESEARCH.md
@.planning/phases/03-prioritized-now-view-distill-daily-file-to-focus-items/03-01-SUMMARY.md

<interfaces>
<!-- Test patterns from existing stubs.test.cjs -->

Path variable declarations at top of file (lines 1-30):
```javascript
const projectRoot = path.join(__dirname, "..");
const adjustToolStubPath = path.join(projectRoot, "stubs", "claude-code", "donna", "adjust-tool.md");
const adjustToolWorkflowPath = path.join(projectRoot, "workflows", "adjust-tool.md");
```

Installer test pattern (lines 1207-1213):
```javascript
it('success message includes "adjust-tool"', () => {
    const content = fs.readFileSync(installerPath, "utf8");
    assert.ok(
        content.includes("adjust-tool"),
        "Installer success message should include adjust-tool skill",
    );
});
```

Stub describe block pattern (lines 1216-1242):
```javascript
describe("stub: stubs/claude-code/donna/adjust-tool.md", () => {
    it("exists", () => {
        assert.ok(fs.existsSync(adjustToolStubPath), "Stub file should exist");
    });
    it('has YAML frontmatter with name "donna:adjust-tool"', () => {
        const content = fs.readFileSync(adjustToolStubPath, "utf8");
        assert.ok(content.startsWith("---"), "Should start with YAML frontmatter delimiter");
        assert.ok(content.includes("name: donna:adjust-tool"), "Should have name");
    });
    it("has description field in frontmatter", () => {
        const content = fs.readFileSync(adjustToolStubPath, "utf8");
        assert.ok(content.includes("description:"), "Should have description field");
    });
    it("contains @~/.donna/workflows/adjust-tool.md reference", () => {
        const content = fs.readFileSync(adjustToolStubPath, "utf8");
        assert.ok(content.includes("@~/.donna/workflows/adjust-tool.md"), "Should reference workflow");
    });
});

describe("workflow: workflows/adjust-tool.md", () => {
    it("exists", () => {
        assert.ok(fs.existsSync(adjustToolWorkflowPath), "Workflow file should exist");
    });
    it("has step structure", () => {
        const content = fs.readFileSync(adjustToolWorkflowPath, "utf8");
        assert.ok(content.includes('step name="read-config"'), "Should have read-config step");
    });
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add focus test blocks to stubs.test.cjs</name>
  <files>test/stubs.test.cjs</files>
  <read_first>
    - test/stubs.test.cjs (full file -- need to see path declarations at top, installer test section, and end of file for append location)
    - stubs/claude-code/donna/focus.md (created by Plan 01 -- verify exact content for test assertions)
    - workflows/focus.md (created by Plan 01 -- verify step names for test assertions)
    - src/installer.cjs (verify "focus" is in success message)
  </read_first>
  <action>
**1. Add path variable declarations** near the top of `test/stubs.test.cjs`, after the existing `adjustToolWorkflowPath` declaration (around line 30):

```javascript
const focusStubPath = path.join(projectRoot, "stubs", "claude-code", "donna", "focus.md");
const focusWorkflowPath = path.join(projectRoot, "workflows", "focus.md");
```

**2. Add installer success message test** in the existing `describe("installer")` block, after the `adjust-tool` test (after line 1213):

```javascript
    it('success message includes "focus"', () => {
        const content = fs.readFileSync(installerPath, "utf8");
        assert.ok(
            content.includes("focus"),
            "Installer success message should include focus skill",
        );
    });
```

**3. Append stub describe block** at the end of the file (after the adjust-tool workflow describe block, after line 1258):

```javascript
describe("stub: stubs/claude-code/donna/focus.md", () => {
    it("exists", () => {
        assert.ok(fs.existsSync(focusStubPath), "Stub file should exist");
    });

    it('has YAML frontmatter with name "donna:focus"', () => {
        const content = fs.readFileSync(focusStubPath, "utf8");
        assert.ok(content.startsWith("---"), "Should start with YAML frontmatter delimiter");
        assert.ok(
            content.includes("name: donna:focus"),
            "Should have name: donna:focus in frontmatter",
        );
    });

    it("has description field in frontmatter", () => {
        const content = fs.readFileSync(focusStubPath, "utf8");
        assert.ok(content.includes("description:"), "Should have description field in frontmatter");
    });

    it("contains @~/.donna/workflows/focus.md reference", () => {
        const content = fs.readFileSync(focusStubPath, "utf8");
        assert.ok(
            content.includes("@~/.donna/workflows/focus.md"),
            "Should reference @~/.donna/workflows/focus.md",
        );
    });

    it("does not have AskUserQuestion -- focus is non-interactive", () => {
        const content = fs.readFileSync(focusStubPath, "utf8");
        assert.ok(
            !content.includes("AskUserQuestion"),
            "Should NOT have AskUserQuestion in allowed-tools",
        );
    });
});

describe("workflow: workflows/focus.md", () => {
    it("exists", () => {
        assert.ok(fs.existsSync(focusWorkflowPath), "Workflow file should exist");
    });

    it("has step structure", () => {
        const content = fs.readFileSync(focusWorkflowPath, "utf8");
        assert.ok(content.includes('step name="read-config"'), "Should have read-config step");
        assert.ok(content.includes('step name="read-daily-file"'), "Should have read-daily-file step");
        assert.ok(content.includes('step name="parse-open-tasks"'), "Should have parse-open-tasks step");
        assert.ok(content.includes('step name="score-and-rank"'), "Should have score-and-rank step");
        assert.ok(content.includes('step name="write-focus-file"'), "Should have write-focus-file step");
        assert.ok(content.includes('step name="print-focus"'), "Should have print-focus step");
    });
});
```

**4. Run `npm run lint:fix`** to ensure the new test code passes linting.
  </action>
  <verify>
    <automated>npm test && npm run lint:fix && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - test/stubs.test.cjs contains `const focusStubPath`
    - test/stubs.test.cjs contains `const focusWorkflowPath`
    - test/stubs.test.cjs contains `success message includes "focus"`
    - test/stubs.test.cjs contains `describe("stub: stubs/claude-code/donna/focus.md"`
    - test/stubs.test.cjs contains `describe("workflow: workflows/focus.md"`
    - test/stubs.test.cjs contains `name: donna:focus`
    - test/stubs.test.cjs contains `@~/.donna/workflows/focus.md`
    - test/stubs.test.cjs contains `AskUserQuestion`
    - test/stubs.test.cjs contains `step name="read-config"`
    - test/stubs.test.cjs contains `step name="score-and-rank"`
    - test/stubs.test.cjs contains `step name="write-focus-file"`
    - `npm test` exits 0 with all tests passing
    - `npm run lint:fix` exits 0 with no errors
  </acceptance_criteria>
  <done>Test file updated with focus stub/workflow test blocks, all tests pass, lint clean</done>
</task>

</tasks>

<verification>
- `npm test` -- all tests pass including new focus tests
- `npm run lint:fix` -- no lint errors
- `grep -q "donna:focus" test/stubs.test.cjs` -- test file references focus skill
- `grep -c "describe(" test/stubs.test.cjs` -- count increased by 2 (stub + workflow)
</verification>

<success_criteria>
- `npm test` exits 0 with all tests passing (including focus stub, workflow, and installer message tests)
- `npm run lint:fix` exits 0
- test/stubs.test.cjs has describe blocks for both focus stub and focus workflow
</success_criteria>

<output>
After completion, create `.planning/phases/03-prioritized-now-view-distill-daily-file-to-focus-items/03-02-SUMMARY.md`
</output>
