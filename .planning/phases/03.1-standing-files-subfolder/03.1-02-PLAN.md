---
phase: 03.1-standing-files-subfolder
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/set-role.md
  - workflows/begin-the-day.md
autonomous: true
requirements:
  - REORG-02
  - REORG-04

must_haves:
  truths:
    - "set-role workflow reads and writes standing files from donna/ subfolder"
    - "begin-the-day workflow reads recurring.md from donna/ subfolder"
    - "Existing users' files are moved from repo root to donna/ on first skill run"
  artifacts:
    - path: "workflows/set-role.md"
      provides: "Updated set-role workflow with donna/ paths and migration guard"
      contains: "<storage_repo>/donna/role.md"
    - path: "workflows/begin-the-day.md"
      provides: "Updated begin-the-day workflow with donna/ paths and migration guard"
      contains: "<storage_repo>/donna/recurring.md"
  key_links:
    - from: "workflows/set-role.md"
      to: "<storage_repo>/donna/"
      via: "all standing file reads/writes use donna/ prefix"
      pattern: "<storage_repo>/donna/"
    - from: "workflows/begin-the-day.md"
      to: "<storage_repo>/donna/recurring.md"
      via: "recurring.md read uses donna/ prefix"
      pattern: "<storage_repo>/donna/recurring.md"
---

<objective>
Update all workflow path references from `<storage_repo>/filename.md` to `<storage_repo>/donna/filename.md` and add a one-time migration guard step to both affected workflows that moves existing standing files from repo root to donna/.

Purpose: After this plan, all skills read/write standing files from the donna/ subfolder, and existing users' files are moved automatically on first skill run post-upgrade.
Output: Updated set-role.md and begin-the-day.md workflows.
</objective>

<execution_context>
@/Users/pingvinen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pingvinen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-standing-files-subfolder/03.1-CONTEXT.md
@.planning/phases/03.1-standing-files-subfolder/03.1-RESEARCH.md

<interfaces>
<!-- Current path references that must change -->

From workflows/set-role.md (standing file paths to update):
- check-existing-role step: `test -f <storage_repo>/role.md` (existence check)
- rerun-menu step: "read current role.md" (Read tool references)
- save-role step: `Write <storage_repo>/role.md` and `Write <storage_repo>/role-research.md`
- save-recurring step: `Write <storage_repo>/recurring.md` and `Read <storage_repo>/recurring.md` (diff-update mode)

From workflows/begin-the-day.md (standing file paths to update):
- check-recurring step: `Read <storage_repo>/recurring.md`
- update-recurring-last-run step: read and write `<storage_repo>/recurring.md`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update set-role.md paths and add migration guard</name>
  <files>workflows/set-role.md</files>
  <read_first>
    - workflows/set-role.md
  </read_first>
  <action>
**Part A: Add migration guard step** — Insert a new step named `migrate-standing-files` immediately after the `read-config` step (before `check-existing-role`). This step detects old-style root-level standing files and moves them to donna/:

```xml
<step name="migrate-standing-files">
Check if standing files exist at the old location (repo root) and move them to donna/ if needed.

Run via Bash:
```bash
STORAGE_REPO="<storage_repo>"
DONNA_DIR="$STORAGE_REPO/donna"

# Only migrate if donna/ doesn't exist yet but root-level standing files do
if [ ! -d "$DONNA_DIR" ] || [ -f "$STORAGE_REPO/role.md" ] || [ -f "$STORAGE_REPO/recurring.md" ] || [ -f "$STORAGE_REPO/role-research.md" ]; then
    mkdir -p "$DONNA_DIR"
    for FILE in role.md recurring.md role-research.md config.md; do
        if [ -f "$STORAGE_REPO/$FILE" ] && [ ! -f "$DONNA_DIR/$FILE" ]; then
            mv "$STORAGE_REPO/$FILE" "$DONNA_DIR/$FILE"
            echo "Moved $FILE to donna/$FILE"
        fi
    done
fi
```

If any files were moved, run:
```bash
git -C <storage_repo> add -A
git -C <storage_repo> diff --cached --quiet || git -C <storage_repo> commit -m "donna(migrate): move standing files to donna/ subfolder"
```

If `auto_push` is true in config, also push.
</step>
```

**Part B: Update all standing file path references** — Find and replace every occurrence (use the exact text, not regex):

1. `<storage_repo>/role.md` -> `<storage_repo>/donna/role.md` (appears in: check-existing-role bash test, rerun-menu Read references, save-role Write)
2. `<storage_repo>/role-research.md` -> `<storage_repo>/donna/role-research.md` (appears in: save-role Write)
3. `<storage_repo>/recurring.md` -> `<storage_repo>/donna/recurring.md` (appears in: save-recurring Write and Read for diff-update)

Do NOT change:
- `~/.config/donna/config.md` — this is the bootstrap config, stays at ~/.config/donna/
- Any references to `~/.donna/` — this is the installer directory, unchanged
- The step name, objective, or overall workflow structure
  </action>
  <verify>
    <automated>cd /Users/pingvinen/workspace/github/donna && grep -c '<storage_repo>/donna/' workflows/set-role.md && ! grep -E '<storage_repo>/(role|recurring|role-research)\.md' workflows/set-role.md | grep -v donna</automated>
  </verify>
  <acceptance_criteria>
    - workflows/set-role.md contains step named `migrate-standing-files`
    - workflows/set-role.md contains `mkdir -p "$DONNA_DIR"`
    - workflows/set-role.md contains `mv "$STORAGE_REPO/$FILE" "$DONNA_DIR/$FILE"`
    - workflows/set-role.md contains `<storage_repo>/donna/role.md` (at least 2 occurrences)
    - workflows/set-role.md contains `<storage_repo>/donna/role-research.md` (at least 1 occurrence)
    - workflows/set-role.md contains `<storage_repo>/donna/recurring.md` (at least 2 occurrences)
    - workflows/set-role.md does NOT contain `<storage_repo>/role.md` without the donna/ prefix (zero occurrences of bare `<storage_repo>/role.md`)
    - workflows/set-role.md does NOT contain `<storage_repo>/recurring.md` without the donna/ prefix
    - workflows/set-role.md does NOT contain `<storage_repo>/role-research.md` without the donna/ prefix
    - workflows/set-role.md still contains `~/.config/donna/config.md` (bootstrap config unchanged)
  </acceptance_criteria>
  <done>set-role.md reads/writes all standing files from donna/ subfolder. Migration guard step moves old-location files on first run.</done>
</task>

<task type="auto">
  <name>Task 2: Update begin-the-day.md paths and add migration guard</name>
  <files>workflows/begin-the-day.md</files>
  <read_first>
    - workflows/begin-the-day.md
  </read_first>
  <action>
**Part A: Add migration guard step** — Insert a new step named `migrate-standing-files` immediately after the `read-config` step (before `get-today`). Use the same pattern as set-role.md:

```xml
<step name="migrate-standing-files">
Check if standing files exist at the old location (repo root) and move them to donna/ if needed.

Run via Bash:
```bash
STORAGE_REPO="<storage_repo>"
DONNA_DIR="$STORAGE_REPO/donna"

# Only migrate if root-level standing files still exist
if [ -f "$STORAGE_REPO/role.md" ] || [ -f "$STORAGE_REPO/recurring.md" ] || [ -f "$STORAGE_REPO/role-research.md" ]; then
    mkdir -p "$DONNA_DIR"
    for FILE in role.md recurring.md role-research.md config.md; do
        if [ -f "$STORAGE_REPO/$FILE" ] && [ ! -f "$DONNA_DIR/$FILE" ]; then
            mv "$STORAGE_REPO/$FILE" "$DONNA_DIR/$FILE"
            echo "Moved $FILE to donna/$FILE"
        fi
    done
fi
```

If any files were moved, run:
```bash
git -C <storage_repo> add -A
git -C <storage_repo> diff --cached --quiet || git -C <storage_repo> commit -m "donna(migrate): move standing files to donna/ subfolder"
```

If `auto_push` is true in config, also push.
</step>
```

**Part B: Update recurring.md path references** — Find and replace:

1. `<storage_repo>/recurring.md` -> `<storage_repo>/donna/recurring.md` (appears in: check-recurring Read, update-recurring-last-run Read and Write)

This is the ONLY standing file path in begin-the-day.md. The workflow does not reference role.md or role-research.md.

Do NOT change:
- `~/.config/donna/config.md` — bootstrap config, unchanged
- Any `<storage_repo>/<daily_folder>/` references — daily files stay in daily/, not donna/
- The step name, objective, or overall workflow structure
  </action>
  <verify>
    <automated>cd /Users/pingvinen/workspace/github/donna && grep -c '<storage_repo>/donna/recurring.md' workflows/begin-the-day.md && ! grep '<storage_repo>/recurring.md' workflows/begin-the-day.md | grep -v donna</automated>
  </verify>
  <acceptance_criteria>
    - workflows/begin-the-day.md contains step named `migrate-standing-files`
    - workflows/begin-the-day.md contains `mkdir -p "$DONNA_DIR"`
    - workflows/begin-the-day.md contains `mv "$STORAGE_REPO/$FILE" "$DONNA_DIR/$FILE"`
    - workflows/begin-the-day.md contains `<storage_repo>/donna/recurring.md` (at least 2 occurrences: check-recurring and update-recurring-last-run)
    - workflows/begin-the-day.md does NOT contain `<storage_repo>/recurring.md` without the donna/ prefix
    - workflows/begin-the-day.md still contains `~/.config/donna/config.md` (bootstrap config unchanged)
    - workflows/begin-the-day.md still contains `<storage_repo>/<daily_folder>/` (daily files unchanged)
  </acceptance_criteria>
  <done>begin-the-day.md reads/writes recurring.md from donna/ subfolder. Migration guard step moves old-location files on first run.</done>
</task>

</tasks>

<verification>
- `grep -r '<storage_repo>/role\.md\|<storage_repo>/recurring\.md\|<storage_repo>/role-research\.md' workflows/ | grep -v donna` returns no results (no bare root-level standing file paths remain)
- Both workflows contain the `migrate-standing-files` step
- Both workflows still reference `~/.config/donna/config.md` for bootstrap config
- `node --test 'test/*.test.cjs'` passes (workflows are markdown, no code tests broken)
</verification>

<success_criteria>
- All standing file references in set-role.md use donna/ prefix
- All recurring.md references in begin-the-day.md use donna/ prefix
- Both workflows have migration guard that moves files from root to donna/
- Migration guard is idempotent (doesn't fail if files already moved or don't exist)
- Migration guard commits the move to git
- No workflow references `<storage_repo>/config.md` (it was never written to the repo)
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-standing-files-subfolder/03.1-02-SUMMARY.md`
</output>
