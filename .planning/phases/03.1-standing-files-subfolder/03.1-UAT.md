---
status: complete
phase: 03.1-standing-files-subfolder
source: [03.1-01-SUMMARY.md, 03.1-02-SUMMARY.md]
started: 2026-03-15T21:30:00Z
updated: 2026-03-15T21:40:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Migration 002 exists and loads
expected: Run the installer test suite (`node --test test/installer.test.cjs`). All 138 tests pass, including migration 002 as a no-op version-marker. Fresh install assertions expect lastMigration=2.
result: issue
reported: "Ran the installer, it upgraded from 0.3.2 -> 0.4.0 and says that it moved the files, but there is no donna/ folder and the standing files are still in the root"
severity: major

### 2. New install creates donna/ directory
expected: In `workflows/setup.md`, the create-storage-structure step includes `mkdir -p <repo>/donna` so new installs get the donna/ subdirectory created alongside daily/.
result: pass

### 3. set-role.md uses donna/ paths
expected: In `workflows/set-role.md`, all standing file references (role.md, role-research.md, recurring.md) point to `<storage_repo>/donna/*.md` instead of root-level `<storage_repo>/*.md`.
result: pass

### 4. begin-the-day.md uses donna/ path
expected: In `workflows/begin-the-day.md`, the recurring.md reference points to `<storage_repo>/donna/recurring.md` instead of root-level `<storage_repo>/recurring.md`.
result: pass

### 5. Migration guard in set-role.md
expected: `workflows/set-role.md` contains a `migrate-standing-files` step that checks for root-level standing files (role.md, role-research.md, recurring.md) and moves them to donna/ if found. The step is idempotent — only moves when source exists and destination does not.
result: pass

### 6. Migration guard in begin-the-day.md
expected: `workflows/begin-the-day.md` contains a `migrate-standing-files` step that checks for root-level standing files and moves them to donna/ if found. Same idempotent pattern as set-role.md.
result: pass

## Summary

total: 6
passed: 5
issues: 1
pending: 0
skipped: 0

## Gaps

- truth: "Migration 002 upgrades cleanly and installer output accurately reflects what happened"
  status: failed
  reason: "User reported: Ran the installer, it upgraded from 0.3.2 -> 0.4.0 and says that it moved the files, but there is no donna/ folder and the standing files are still in the root"
  severity: major
  test: 1
  artifacts: []
  missing: []
