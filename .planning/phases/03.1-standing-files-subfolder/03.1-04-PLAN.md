---
phase: 03.1-standing-files-subfolder
plan: 04
type: execute
wave: 2
depends_on: [03.1-03]
files_modified:
  - workflows/set-role.md
  - workflows/begin-the-day.md
  - workflows/add-task.md
  - workflows/done.md
autonomous: true
gap_closure: true
requirements: [REORG-04]

must_haves:
  truths:
    - "All four workflows (set-role, begin-the-day, add-task, done) check for pending migrations after read-config"
    - "When move-standing-files flag is found, files are moved from repo root to donna/ and the flag is cleared"
    - "A user who upgrades and only runs add-task or done will still get their files migrated"
    - "The dedicated migrate-standing-files steps in set-role and begin-the-day are replaced by the shared check"
  artifacts:
    - path: "workflows/set-role.md"
      provides: "check-pending-migrations step replacing migrate-standing-files"
      contains: "check-pending-migrations"
    - path: "workflows/begin-the-day.md"
      provides: "check-pending-migrations step replacing migrate-standing-files"
      contains: "check-pending-migrations"
    - path: "workflows/add-task.md"
      provides: "check-pending-migrations step after read-config"
      contains: "check-pending-migrations"
    - path: "workflows/done.md"
      provides: "check-pending-migrations step after read-config"
      contains: "check-pending-migrations"
  key_links:
    - from: "workflows/add-task.md"
      to: "~/.donna/state.md"
      via: "Read tool in check-pending-migrations step"
      pattern: "state\\.md"
    - from: "workflows/done.md"
      to: "~/.donna/state.md"
      via: "Read tool in check-pending-migrations step"
      pattern: "state\\.md"
---

<objective>
Add a shared `check-pending-migrations` step to all four workflows so that any skill run after an upgrade will execute pending storage repo migrations (specifically, moving standing files to donna/).

Purpose: Currently only set-role and begin-the-day have migration guards. If a user upgrades and only uses add-task or done, standing files never move. This plan adds a generic pending-migration check to all workflows, replacing the ad-hoc guards.
Output: All four workflows have check-pending-migrations step; old migrate-standing-files steps removed from set-role and begin-the-day.
</objective>

<execution_context>
@/Users/pingvinen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pingvinen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-standing-files-subfolder/03.1-02-SUMMARY.md
@.planning/phases/03.1-standing-files-subfolder/03.1-03-SUMMARY.md

<interfaces>
<!-- state.md format written by migration 002 (from plan 03) -->
```markdown
---
pending_migrations:
  - move-standing-files
---
```

<!-- The check-pending-migrations step must be identical across all 4 workflows.
     It reads ~/.donna/state.md, checks for pending_migrations, and for each:
     - move-standing-files: mkdir donna/, mv root standing files, git commit, clear flag -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace migrate-standing-files with check-pending-migrations in set-role.md and begin-the-day.md</name>
  <files>workflows/set-role.md, workflows/begin-the-day.md</files>
  <action>
In BOTH workflows, replace the existing `<step name="migrate-standing-files">` with a new `<step name="check-pending-migrations">` that uses a generic pattern.

The new step (inserted right after the `read-config` step in both files):

```xml
<step name="check-pending-migrations">
Read `~/.donna/state.md` with the Read tool. If the file does not exist or has no `pending_migrations` field in its YAML frontmatter, skip this step.

For each entry in `pending_migrations`:

**`move-standing-files`:** Move standing files from storage repo root to donna/ subfolder.

Run via Bash:
```bash
STORAGE_REPO="<storage_repo>"
DONNA_DIR="$STORAGE_REPO/donna"
MOVED=0

mkdir -p "$DONNA_DIR"
for FILE in role.md recurring.md role-research.md; do
    if [ -f "$STORAGE_REPO/$FILE" ] && [ ! -f "$DONNA_DIR/$FILE" ]; then
        mv "$STORAGE_REPO/$FILE" "$DONNA_DIR/$FILE"
        echo "Moved $FILE to donna/$FILE"
        MOVED=$((MOVED + 1))
    fi
done

echo "MOVED=$MOVED"
```

If MOVED > 0, commit the move:
```bash
git -C <storage_repo> add -A
git -C <storage_repo> diff --cached --quiet || git -C <storage_repo> commit -m "donna(migrate): move standing files to donna/ subfolder"
```

If `auto_push` is true in config, also push.

After processing all pending migrations, update `~/.donna/state.md` with the Write tool: remove the completed entries from `pending_migrations`. If no entries remain, write:
```markdown
---
pending_migrations: []
---
```
</step>
```

IMPORTANT: Remove the entire old `<step name="migrate-standing-files">` from both files. Do not leave both steps.

In set-role.md, the old step is between `read-config` and `check-existing-role`.
In begin-the-day.md, the old step is between `read-config` and `get-today`.
  </action>
  <verify>
    <automated>grep -c "check-pending-migrations" workflows/set-role.md workflows/begin-the-day.md && ! grep -c "migrate-standing-files" workflows/set-role.md workflows/begin-the-day.md</automated>
  </verify>
  <done>Both workflows have check-pending-migrations step. Neither workflow has the old migrate-standing-files step. The step content is identical in both files.</done>
</task>

<task type="auto">
  <name>Task 2: Add check-pending-migrations step to add-task.md and done.md</name>
  <files>workflows/add-task.md, workflows/done.md</files>
  <action>
Add the EXACT SAME `<step name="check-pending-migrations">` step (identical content to what was written in Task 1) to both workflows, inserted right after the `read-config` step.

In add-task.md: insert between `read-config` and `get-description`.
In done.md: insert between `read-config` and `find-daily-file`.

The step content must be character-for-character identical to the one in set-role.md and begin-the-day.md — copy it exactly. This ensures any future migration handler added to one workflow works in all four.
  </action>
  <verify>
    <automated>grep -c "check-pending-migrations" workflows/add-task.md workflows/done.md</automated>
  </verify>
  <done>All four workflows (set-role.md, begin-the-day.md, add-task.md, done.md) contain the identical check-pending-migrations step immediately after read-config. A user running any skill after upgrade will trigger the standing file move.</done>
</task>

</tasks>

<verification>
- `grep -l "check-pending-migrations" workflows/*.md` returns all four workflow files
- `grep -l "migrate-standing-files" workflows/*.md` returns nothing (old step fully removed)
- Visual inspection: the check-pending-migrations step is identical across all four files
</verification>

<success_criteria>
- All four workflows contain check-pending-migrations step after read-config
- Old migrate-standing-files steps removed from set-role.md and begin-the-day.md
- The step reads ~/.donna/state.md, processes move-standing-files flag, clears the flag after completion
- A user who upgrades and runs only /donna:add-task will have their standing files moved automatically
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-standing-files-subfolder/03.1-04-SUMMARY.md`
</output>
