---
phase: 01-packaging-and-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - biome.json
  - src/output.cjs
  - src/version.cjs
  - src/migrator.cjs
  - src/providers/claude-code.cjs
  - src/providers/index.cjs
  - migrations/001-initial.cjs
  - stubs/claude-code/donna/setup.md
  - workflows/setup.md
  - test/version.test.cjs
  - test/migrator.test.cjs
  - test/stubs.test.cjs
  - test/package.test.cjs
autonomous: true
requirements:
  - DIST-02
  - DIST-03
  - DIST-05
  - DIST-06

must_haves:
  truths:
    - "Package.json has correct name, bin, files, engines, publishConfig, and scripts"
    - "Version file can be read and written in the documented markdown format"
    - "Migrator runs numbered .cjs files in order, stops on failure, reports results"
    - "Provider detection finds Claude Code when ~/.claude/ exists"
    - "Stub file has correct YAML frontmatter and @ path reference to workflow"
    - "Workflow file produces hello-world output with version display"
    - "All test files pass with node --test test/"
  artifacts:
    - path: "package.json"
      provides: "npm package definition"
      contains: "@pingvinen/donna-assistant"
    - path: "src/version.cjs"
      provides: "Version file read/write"
      exports: ["readVersion", "writeVersion"]
    - path: "src/migrator.cjs"
      provides: "Migration runner"
      exports: ["runMigrations"]
    - path: "src/providers/index.cjs"
      provides: "Provider aggregator"
      exports: ["detectProviders"]
    - path: "stubs/claude-code/donna/setup.md"
      provides: "donna:setup stub for Claude Code"
      contains: "donna:setup"
    - path: "workflows/setup.md"
      provides: "Setup workflow loaded by stub"
      contains: "DONNA"
  key_links:
    - from: "stubs/claude-code/donna/setup.md"
      to: "workflows/setup.md"
      via: "@~/.donna/workflows/setup.md reference"
      pattern: "@~/.donna/workflows/setup.md"
    - from: "migrations/001-initial.cjs"
      to: "src/migrator.cjs"
      via: "migrator requires and executes migration files"
      pattern: "module.exports.*version.*description.*up"
---

<objective>
Create the npm package foundation with all core modules, content files, and test scaffolds.

Purpose: Establishes the package structure, all independent modules (output, version, migrator, providers), the donna:setup stub and workflow, the initial migration, and comprehensive test coverage. Plan 02 wires these modules into the installer.

Output: A buildable npm package with all modules individually tested, ready for integration in Plan 02.
</objective>

<execution_context>
@/Users/pingvinen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pingvinen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-packaging-and-distribution/01-CONTEXT.md
@.planning/phases/01-packaging-and-distribution/01-RESEARCH.md
@.planning/phases/01-packaging-and-distribution/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Package config, output helpers, version module, and their tests</name>
  <files>package.json, biome.json, src/output.cjs, src/version.cjs, test/version.test.cjs, test/package.test.cjs</files>
  <behavior>
    - Test (version): readVersion returns null when file does not exist
    - Test (version): readVersion parses version, lastMigration, installed, updated from markdown format
    - Test (version): writeVersion creates correct markdown with version, lastMigration, timestamps
    - Test (version): writeVersion on existing file updates "Updated" timestamp but preserves "Installed" timestamp
    - Test (package): package.json has name "@pingvinen/donna-assistant"
    - Test (package): package.json bin field maps "donna-assistant" to "./bin/install.cjs"
    - Test (package): package.json files field includes bin/, src/, stubs/, workflows/, migrations/, templates/, references/
    - Test (package): package.json engines requires node >= 18
    - Test (package): package.json publishConfig has access "public"
  </behavior>
  <action>
    1. Create package.json per the RESEARCH.md code example (name: @pingvinen/donna-assistant, version: 0.1.0, bin, files, engines, publishConfig, scripts for lint/test/prepublishOnly, devDependency on @biomejs/biome).
    2. Create biome.json with default settings (formatter + linter enabled, indent: 2 spaces, line width: 100).
    3. Run `npm install` to install biome dev dependency.
    4. Create src/output.cjs: export helper functions for formatted console output. Functions: `banner()` prints the DONNA header with `━━━` lines, `success(msg)` prints `  ✓ {msg}`, `fail(msg)` prints `  ✗ {msg}`, `info(msg)` prints `  {msg}`, `upgradeHeader(from, to)` prints `  Upgrading {from} → {to}:`, `migrationLine(desc)` prints `    • {desc}`. Use raw ANSI-free strings (no chalk dependency). All output goes to stdout.
    5. Create src/version.cjs: export `readVersion(donnaDir)` and `writeVersion(donnaDir, version, lastMigration)`. readVersion returns `{ version, lastMigration, installed, updated }` or null if file missing. writeVersion creates/updates `~/.donna/version.md` in the documented markdown format (see RESEARCH.md Pattern 4). On update (file already exists), preserve the original "Installed" timestamp and only update "Updated". Stage after each file is complete.
    6. Write tests RED first, then make them GREEN.
  </action>
  <verify>
    <automated>node --test test/version.test.cjs test/package.test.cjs</automated>
  </verify>
  <done>package.json valid with all required fields, biome installed, output helpers exported, version read/write works with correct markdown format, all tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrator, provider detection, and their tests</name>
  <files>src/migrator.cjs, src/providers/claude-code.cjs, src/providers/index.cjs, migrations/001-initial.cjs, test/migrator.test.cjs</files>
  <behavior>
    - Test (migrator): returns empty array when no migrations pending
    - Test (migrator): runs migrations in numeric order (001 before 002 before 010)
    - Test (migrator): skips migrations at or below lastMigration number
    - Test (migrator): stops on first failure and returns partial results with error
    - Test (migrator): each result has num, description, ok, and optionally error
    - Test (migrator): migration up() receives ctx with donnaDir, fs, path, os
    - Test (provider): detectProviders returns Claude Code when ~/.claude/ dir exists (use temp dir)
    - Test (provider): detectProviders returns empty array when no provider dirs exist
    - Test (provider): each detected provider has name, stubSource, stubTarget
  </behavior>
  <action>
    1. Create src/migrator.cjs: export `runMigrations(migrationsDir, donnaDir, lastMigration)`. Read .cjs files from migrationsDir, sort by numeric prefix (parseInt), filter to those > lastMigration, execute sequentially. Each migration receives `ctx = { donnaDir, fs, path, os }`. Return array of `{ num, description, ok, error? }`. Stop on first failure.
    2. Create src/providers/claude-code.cjs: export a provider object `{ name: 'Claude Code', detect(homeDir), stubSource, getStubTarget(homeDir) }`. detect() checks if `{homeDir}/.claude/` exists. stubSource points to `stubs/claude-code/` relative to package root. getStubTarget returns `{homeDir}/.claude/commands/`.
    3. Create src/providers/index.cjs: import claude-code provider, export `detectProviders(homeDir)` which returns array of providers where detect() is true.
    4. Create migrations/001-initial.cjs: exports `{ version: '0.1.0', description: 'Initial directory structure', up(ctx) }`. up() creates directories: workflows/, templates/, references/ inside ctx.donnaDir using fs.mkdirSync with recursive:true.
    5. Write tests RED first using temp directories (fs.mkdtempSync), then make GREEN. For migrator tests, create test migration files in a temp dir. For provider tests, create/omit a `.claude/` dir in a temp home dir.
  </action>
  <verify>
    <automated>node --test test/migrator.test.cjs</automated>
  </verify>
  <done>Migrator runs numbered migrations cumulatively with stop-on-failure, provider detection finds Claude Code by directory existence, 001-initial migration creates subdirectories, all tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stub and workflow content files with tests</name>
  <files>stubs/claude-code/donna/setup.md, workflows/setup.md, templates/.gitkeep, references/.gitkeep, test/stubs.test.cjs</files>
  <behavior>
    - Test (stub): stubs/claude-code/donna/setup.md exists
    - Test (stub): stub has YAML frontmatter with name "donna:setup"
    - Test (stub): stub has description field in frontmatter
    - Test (stub): stub contains @~/.donna/workflows/setup.md reference
    - Test (workflow): workflows/setup.md exists
    - Test (workflow): workflow contains DONNA banner section
    - Test (workflow): workflow references version.md for version display
    - Test (workflow): workflow mentions "stub" indicating Phase 2 will add real logic
  </behavior>
  <action>
    1. Create stubs/claude-code/donna/setup.md following the GSD stub pattern from RESEARCH.md Pattern 2:
       - YAML frontmatter: name: donna:setup, description: "Set up Donna assistant for this machine", allowed-tools: [Read, Bash]
       - objective section explaining what the command does
       - execution_context with `@~/.donna/workflows/setup.md`
    2. Create workflows/setup.md — the workflow that the stub loads. Content:
       - Prints DONNA banner (━━━ DONNA ▸ Setup ━━━)
       - Reads ~/.donna/version.md and displays installed version
       - Prints "This is a stub — real setup coming in Phase 2"
       - Prints next steps (e.g., "Run /donna:setup again after upgrading to see new features")
       - Uses Claude instruction format (objective, steps) so Claude Code can execute it
    3. Create templates/.gitkeep and references/.gitkeep (empty dirs for Phase 1, used later).
    4. Write tests RED first, then GREEN. Tests read file contents and validate structure.
  </action>
  <verify>
    <automated>node --test test/stubs.test.cjs</automated>
  </verify>
  <done>Stub file has correct frontmatter and @ reference, workflow produces hello-world with version display, templates/ and references/ directories exist, all tests green</done>
</task>

</tasks>

<verification>
All unit tests pass: `node --test test/`
Biome lint passes: `npx biome check .`
Package contains all required files: `npm pack --dry-run` lists bin/, src/, stubs/, workflows/, migrations/, templates/, references/
</verification>

<success_criteria>
- package.json is valid with correct name, bin, files, engines, publishConfig
- All 4 src modules export their documented functions
- Stub and workflow files exist with correct content and cross-references
- 001-initial migration creates the right directories
- All test files pass
- Biome lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-packaging-and-distribution/01-01-SUMMARY.md`
</output>
